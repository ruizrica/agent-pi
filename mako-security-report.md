# Security Report — agent-pi

**Scan Date:** Apr 4, 2026 at 07:20 AM  
**Total Findings:** 246  

## Summary

The scan of **agent-pi** identified **164 findings requiring action**, including **7 critical** issues that should be addressed immediately. Of the 246 total findings, 82 were automatically triaged (0 false positives, 82 accepted risk), leaving 164 for review. The most common issues are **innerHTML Assignment** (107), **Regular Expression Denial of Service (ReDoS)** (19), **CORS Wildcard with Credentials** (17). **Immediate actions:** patch critical vulnerabilities; parameterize SQL queries; restrict CORS origins; sanitize HTML output with DOMPurify.

| Category | Count |
|----------|-------|
| Critical | 7 |
| High | 231 |
| Medium | 7 |
| Low | 1 |
| **Total** | **246** |

| Triage | Count |
|--------|-------|
| Action Required | **164** |
| Likely False Positive | 0 |
| Accepted Risk | 82 |

## Action Required (164)

These findings require review and remediation:

### [!] CRITICAL (7)

#### Dynamic Code Execution from LLM Output

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/cleanup-viewer-html.ts:908`  
**Rule:** [`SAST-AI-TOOL-002`](https://owasp.org/www-project-top-10-for-large-language-model-applications/)  
**CWE:** [CWE-94](https://cwe.mitre.org/data/definitions/94.html)  
**OWASP:** [A03:2021](https://owasp.org/Top10/A03_2021-Injection/)  

> Executing code generated by an LLM (via eval, exec, Function constructor, or code interpreter) without sandboxing enables remote code execution through prompt injection

**Source Context:**
```
   903 |       var reader = res.body.getReader();
   904 |       var decoder = new TextDecoder();
   905 |       var fullText = '';
   906 | 
   907 |       function read() {
►  908 |         reader.read().then(function(result) {
   909 |           if (result.done) {
   910 |             var cursor = body.querySelector('.ai-cursor');
   911 |             if (cursor) cursor.remove();
   912 |             return;
   913 |           }
```

**Remediation:**
Never execute LLM-generated code directly. Use a sandboxed runtime (VM2, Pyodide, Docker) with strict resource limits

---

#### Unsafe LLM Output Rendering

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/file-viewer-html.ts:885`  
**Rule:** [`SAST-AI-OUT-001`](https://owasp.org/www-project-top-10-for-large-language-model-applications/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021](https://owasp.org/Top10/A03_2021-Injection/)  

> LLM-generated content rendered as HTML, executed as code, or set via innerHTML without sanitization enables XSS and code injection

**Source Context:**
```
   880 |       if (lang) {
   881 |         result = hljs.highlight(currentContent, { language: lang });
   882 |       } else {
   883 |         result = hljs.highlightAuto(currentContent);
   884 |       }
►  885 |       codeBlock.innerHTML = result.value;
   886 |       codeBlock.className = (lang ? 'language-' + lang + ' ' : '') + 'hljs';
   887 |     } else {
   888 |       codeBlock.textContent = currentContent;
   889 |     }
   890 |     lastHighlightedContent = currentContent;
```

**Remediation:**
Sanitize LLM output with DOMPurify before rendering as HTML. LLM responses can contain injected scripts

---

#### eval() Usage

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/report-index.ts:310`  
**Rule:** [`SAST-INJ-006`](https://owasp.org/Top10/A03_2021-Injection/)  
**CWE:** [CWE-95](https://cwe.mitre.org/data/definitions/95.html)  

> eval() executes arbitrary code and should never be used with user input

**Source Context:**
```
   305 | 	const tx = database.transaction((entries: PersistedReportEntry[]) => {
   306 | 		if (ids.length > 0) {
   307 | 			const placeholders = ids.map(() => "?").join(", ");
   308 | 			database.prepare(`DELETE FROM reports WHERE id NOT IN (${placeholders})`).run(...ids);
   309 | 		} else {
►  310 | 			database.exec("DELETE FROM reports");
   311 | 		}
   312 | 		for (const rawEntry of entries) {
   313 | 			const entry = normalizeEntry(rawEntry as any);
   314 | 			replace.run(
   315 | 				entry.id,
```

**Remediation:**
Avoid exec(). Use ast.literal_eval() for safe evaluation

---

#### eval() Usage

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/secure-engine.ts:207`  
**Rule:** [`SAST-INJ-006`](https://owasp.org/Top10/A03_2021-Injection/)  
**CWE:** [CWE-95](https://cwe.mitre.org/data/definitions/95.html)  

> eval() executes arbitrary code and should never be used with user input

**Source Context:**
```
   202 | 	{
   203 | 		pattern: /(?:eval|Function|new\s+Function)\s*\(.*(?:response|completion|output|result|content|text)/is,
   204 | 		severity: "critical",
   205 | 		category: "unsafe_eval",
   206 | 		title: "AI output passed to eval/Function constructor",
►  207 | 		description: "AI-generated output is being evaluated as code via eval() or the Function constructor. This is extremely dangerous — a prompt injection could lead to arbitrary code execution.",
   208 | 		recommendation: "Never eval() AI outputs. Use structured output parsing (JSON.parse with validation) instead.",
   209 | 	},
   210 | 	{
   211 | 		pattern: /(?:exec|execSync|spawn|spawnSync|execFile)\s*\(.*(?:response|completion|output|result|content|generated)/is,
   212 | 		severity: "critical",
```

**Remediation:**
Avoid eval(). Use safer alternatives like Function() with strict validation

---

#### eval() Usage

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/secure-engine.ts:208`  
**Rule:** [`SAST-INJ-006`](https://owasp.org/Top10/A03_2021-Injection/)  
**CWE:** [CWE-95](https://cwe.mitre.org/data/definitions/95.html)  

> eval() executes arbitrary code and should never be used with user input

**Source Context:**
```
   203 | 		pattern: /(?:eval|Function|new\s+Function)\s*\(.*(?:response|completion|output|result|content|text)/is,
   204 | 		severity: "critical",
   205 | 		category: "unsafe_eval",
   206 | 		title: "AI output passed to eval/Function constructor",
   207 | 		description: "AI-generated output is being evaluated as code via eval() or the Function constructor. This is extremely dangerous — a prompt injection could lead to arbitrary code execution.",
►  208 | 		recommendation: "Never eval() AI outputs. Use structured output parsing (JSON.parse with validation) instead.",
   209 | 	},
   210 | 	{
   211 | 		pattern: /(?:exec|execSync|spawn|spawnSync|execFile)\s*\(.*(?:response|completion|output|result|content|generated)/is,
   212 | 		severity: "critical",
   213 | 		category: "unsafe_eval",
```

**Remediation:**
Avoid eval(). Use safer alternatives like Function() with strict validation

---

#### eval() Usage

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/tool-caller.ts:242`  
**Rule:** [`SAST-INJ-006`](https://owasp.org/Top10/A03_2021-Injection/)  
**CWE:** [CWE-95](https://cwe.mitre.org/data/definitions/95.html)  

> eval() executes arbitrary code and should never be used with user input

**Source Context:**
```
   237 | 			if (!command) return { content: [{ type: "text", text: "Error: 'command' parameter required" }] };
   238 | 			const timeout = (args.timeout as number) || undefined;
   239 | 			try {
   240 | 				// pi.exec takes (binary, args[], options) like child_process.spawn
   241 | 				// For shell commands, we need to invoke bash -c "command"
►  242 | 				const result = await pi.exec("bash", ["-c", command], {
   243 | 					signal,
   244 | 					timeout: timeout ? timeout * 1000 : undefined,
   245 | 					cwd,
   246 | 				});
   247 | 				const output = result.stdout + (result.stderr ? `\nSTDERR: ${result.stderr}` : "");
```

**Remediation:**
Avoid exec(). Use ast.literal_eval() for safe evaluation

---

#### Insecure Deserialization

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/skills/expo-cicd-workflows/scripts/validate.js:32`  
**Rule:** [`SAST-INJ-008`](https://owasp.org/Top10/A03_2021-Injection/)  
**CWE:** [CWE-502](https://cwe.mitre.org/data/definitions/502.html)  
**OWASP:** [A08:2021](https://owasp.org/Top10/A08_2021-Software_and_Data_Integrity_Failures/)  

> Deserializing untrusted data can lead to remote code execution

**Source Context:**
```
    27 | async function validateFile(validator, filePath) {
    28 |   const content = await readFile(filePath, 'utf-8');
    29 | 
    30 |   let doc;
    31 |   try {
►   32 |     doc = yaml.load(content);
    33 |   } catch (e) {
    34 |     return { valid: false, error: `YAML parse error: ${e.message}` };
    35 |   }
    36 | 
    37 |   const valid = validator(doc);
```

**Remediation:**
Use yaml.safe_load() instead of yaml.load()

---

### [H] HIGH (150)

#### CORS Wildcard with Credentials

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/board-viewer.ts:154`  
**Rule:** [`SAST-CFG-001`](https://owasp.org/Top10/A05_2021-Security_Misconfiguration/)  
**CWE:** [CWE-942](https://cwe.mitre.org/data/definitions/942.html)  

> CORS with origin: * and credentials: true allows any origin to make authenticated requests

**Source Context:**
```
   149 | 		const resultPromise = new Promise<BoardResult>((res) => {
   150 | 			resolveResult = res;
   151 | 		});
   152 | 
   153 | 		const server = createServer(async (req: IncomingMessage, res: ServerResponse) => {
►  154 | 			res.setHeader("Access-Control-Allow-Origin", "*");
   155 | 			res.setHeader("Access-Control-Allow-Methods", "GET, POST, OPTIONS");
   156 | 			res.setHeader("Access-Control-Allow-Headers", "Content-Type");
   157 | 
   158 | 			if (req.method === "OPTIONS") {
   159 | 				res.writeHead(204);
```

**Remediation:**
Specify explicit origins for CORS instead of using wildcard (*)

---

#### CORS Wildcard with Credentials

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/cleanup-viewer.ts:274`  
**Rule:** [`SAST-CFG-001`](https://owasp.org/Top10/A05_2021-Security_Misconfiguration/)  
**CWE:** [CWE-942](https://cwe.mitre.org/data/definitions/942.html)  

> CORS with origin: * and credentials: true allows any origin to make authenticated requests

**Source Context:**
```
   269 | 			resolveResult!(result);
   270 | 		};
   271 | 		const resultPromise = new Promise<CleanupResult>((res) => { resolveResult = res; });
   272 | 
   273 | 		const server = createServer(async (req: IncomingMessage, res: ServerResponse) => {
►  274 | 			res.setHeader("Access-Control-Allow-Origin", "*");
   275 | 			res.setHeader("Access-Control-Allow-Methods", "GET, POST, OPTIONS");
   276 | 			res.setHeader("Access-Control-Allow-Headers", "Content-Type");
   277 | 
   278 | 			if (req.method === "OPTIONS") { res.writeHead(204); res.end(); return; }
   279 | 
```

**Remediation:**
Specify explicit origins for CORS instead of using wildcard (*)

---

#### CORS Wildcard with Credentials

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/completion-report.ts:291`  
**Rule:** [`SAST-CFG-001`](https://owasp.org/Top10/A05_2021-Security_Misconfiguration/)  
**CWE:** [CWE-942](https://cwe.mitre.org/data/definitions/942.html)  

> CORS with origin: * and credentials: true allows any origin to make authenticated requests

**Source Context:**
```
   286 | 		const resultPromise = new Promise<ReportResult>((res) => {
   287 | 			resolveResult = res;
   288 | 		});
   289 | 
   290 | 		const server = createServer((req: IncomingMessage, res: ServerResponse) => {
►  291 | 			res.setHeader("Access-Control-Allow-Origin", "*");
   292 | 			res.setHeader("Access-Control-Allow-Methods", "GET, POST, OPTIONS");
   293 | 			res.setHeader("Access-Control-Allow-Headers", "Content-Type");
   294 | 
   295 | 			if (req.method === "OPTIONS") {
   296 | 				res.writeHead(204);
```

**Remediation:**
Specify explicit origins for CORS instead of using wildcard (*)

---

#### Regular Expression Denial of Service (ReDoS)

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/complex-problem-loop.ts:52`  
**Rule:** [`SAST-ADV-003`](https://owasp.org/Top10/A09_2021-Security_Logging_and_Monitoring_Failures/)  
**CWE:** [CWE-1333](https://cwe.mitre.org/data/definitions/1333.html)  
**OWASP:** [A06:2021](https://owasp.org/Top10/A06_2021-Vulnerable_and_Outdated_Components/)  

> Unsanitized input used in regular expression constructors can cause catastrophic backtracking and denial of service

**Source Context:**
```
    47 |   return items.map((item) => `- ${item}`).join("\n");
    48 | }
    49 | 
    50 | function upsertSection(markdown: string, title: string, content: string): string {
    51 |   const header = `## ${title}`;
►   52 |   const pattern = new RegExp(`(^## ${title}\\n)([\\s\\S]*?)(?=\\n## [^\\n]+|$)`, "m");
    53 |   if (pattern.test(markdown)) {
    54 |     return markdown.replace(pattern, `${header}\n${content.trim()}\n`);
    55 |   }
    56 |   const trimmed = markdown.trimEnd();
    57 |   return `${trimmed}\n\n${header}\n${content.trim()}\n`;
```

**Remediation:**
Never construct regex from user input. Use string methods or escape special characters with escape-string-regexp

---

#### SQL Injection via String Concatenation

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/complex-problem-loop.ts:118`  
**Rule:** [`SAST-INJ-001`](https://owasp.org/Top10/A03_2021-Injection/)  
**CWE:** [CWE-89](https://cwe.mitre.org/data/definitions/89.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> SQL query built with string concatenation and variables may be vulnerable to injection

**Source Context:**
```
   113 |       let updated = existing;
   114 |       if (current_understanding) {
   115 |         updated = upsertSection(updated, "Current Understanding", bulletLines(current_understanding));
   116 |       }
   117 |       if (active_hypothesis) {
►  118 |         updated = upsertSection(updated, "Active Hypothesis", `- ${active_hypothesis}`);
   119 |       }
   120 |       if (open_questions) {
   121 |         updated = upsertSection(updated, "Open Questions", bulletLines(open_questions));
   122 |       }
   123 |       if (risks) {
```

**Remediation:**
Use parameterized queries with placeholders ($1, ?, :param) instead of string interpolation

---

#### SQL Injection via String Concatenation

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/complex-problem-loop.ts:127`  
**Rule:** [`SAST-INJ-001`](https://owasp.org/Top10/A03_2021-Injection/)  
**CWE:** [CWE-89](https://cwe.mitre.org/data/definitions/89.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> SQL query built with string concatenation and variables may be vulnerable to injection

**Source Context:**
```
   122 |       }
   123 |       if (risks) {
   124 |         updated = upsertSection(updated, "Risks", bulletLines(risks));
   125 |       }
   126 |       if (last_slice) {
►  127 |         updated = upsertSection(updated, "Last Slice", `- ${last_slice}`);
   128 |       }
   129 |       updated = upsertSection(updated, "Next Slice", `- ${next_slice}`);
   130 |       writeFileSync(filePath, updated, "utf-8");
   131 |       return { content: [{ type: "text" as const, text: `Updated complex problem session at ${filePath}` }] };
   132 |     },
```

**Remediation:**
Use parameterized queries with placeholders ($1, ?, :param) instead of string interpolation

---

#### SQL Injection via String Concatenation

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/complex-problem-loop.ts:129`  
**Rule:** [`SAST-INJ-001`](https://owasp.org/Top10/A03_2021-Injection/)  
**CWE:** [CWE-89](https://cwe.mitre.org/data/definitions/89.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> SQL query built with string concatenation and variables may be vulnerable to injection

**Source Context:**
```
   124 |         updated = upsertSection(updated, "Risks", bulletLines(risks));
   125 |       }
   126 |       if (last_slice) {
   127 |         updated = upsertSection(updated, "Last Slice", `- ${last_slice}`);
   128 |       }
►  129 |       updated = upsertSection(updated, "Next Slice", `- ${next_slice}`);
   130 |       writeFileSync(filePath, updated, "utf-8");
   131 |       return { content: [{ type: "text" as const, text: `Updated complex problem session at ${filePath}` }] };
   132 |     },
   133 |   });
   134 | }
```

**Remediation:**
Use parameterized queries with placeholders ($1, ?, :param) instead of string interpolation

---

#### CORS Wildcard with Credentials

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/file-viewer.ts:130`  
**Rule:** [`SAST-CFG-001`](https://owasp.org/Top10/A05_2021-Security_Misconfiguration/)  
**CWE:** [CWE-942](https://cwe.mitre.org/data/definitions/942.html)  

> CORS with origin: * and credentials: true allows any origin to make authenticated requests

**Source Context:**
```
   125 | 		const resultPromise = new Promise<FileViewerResult>((res) => {
   126 | 			resolveResult = res;
   127 | 		});
   128 | 
   129 | 		const server = createServer((req: IncomingMessage, res: ServerResponse) => {
►  130 | 			res.setHeader("Access-Control-Allow-Origin", "*");
   131 | 			res.setHeader("Access-Control-Allow-Methods", "GET, POST, OPTIONS");
   132 | 			res.setHeader("Access-Control-Allow-Headers", "Content-Type");
   133 | 
   134 | 			if (req.method === "OPTIONS") {
   135 | 				res.writeHead(204);
```

**Remediation:**
Specify explicit origins for CORS instead of using wildcard (*)

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/board-viewer-html.ts:972`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   967 | 
   968 |   // ── Render Agents ────────────────────
   969 |   function renderAgents(agents) {
   970 |     // Remove old chips (keep label)
   971 |     const label = agentStrip.querySelector('.agent-strip-label');
►  972 |     agentStrip.innerHTML = '';
   973 |     agentStrip.appendChild(label);
   974 | 
   975 |     if (agents.length === 0) {
   976 |       const empty = document.createElement('span');
   977 |       empty.className = 'no-agents';
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/board-viewer-html.ts:991`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   986 | 
   987 |     for (const agent of agents) {
   988 |       const chip = document.createElement('div');
   989 |       const state = (agent.state || 'idle').toLowerCase();
   990 |       chip.className = 'agent-chip state-' + state + (selectedAgent === agent.name ? ' selected' : '');
►  991 |       chip.innerHTML = '<span class="chip-dot"></span>' +
   992 |         escapeHtml(agent.name || 'unnamed') +
   993 |         (agent.role ? ' <span style="opacity:0.6;font-size:10px">(' + escapeHtml(agent.role) + ')</span>' : '');
   994 |       chip.onclick = () => toggleAgentFilter(agent.name);
   995 |       chip.title = 'State: ' + state + (agent.agent_type ? ' | Type: ' + agent.agent_type : '');
   996 |       agentStrip.appendChild(chip);
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/board-viewer-html.ts:1034`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
  1029 |     for (const [status, col] of Object.entries(columns)) {
  1030 |       const items = buckets[status] || [];
  1031 |       col.count.textContent = items.length;
  1032 | 
  1033 |       if (items.length === 0) {
► 1034 |         col.body.innerHTML = '<div class="column-empty">No ' + status + ' tasks</div>';
  1035 |         continue;
  1036 |       }
  1037 | 
  1038 |       // Limit display to 50 per column
  1039 |       const visible = items.slice(0, 50);
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/board-viewer-html.ts:1040`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
  1035 |         continue;
  1036 |       }
  1037 | 
  1038 |       // Limit display to 50 per column
  1039 |       const visible = items.slice(0, 50);
► 1040 |       col.body.innerHTML = '';
  1041 | 
  1042 |       for (const task of visible) {
  1043 |         const card = document.createElement('div');
  1044 |         const isExpanded = expandedCards.has(task.task_id);
  1045 |         card.className = 'task-card' + (isExpanded ? ' expanded' : '');
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/board-viewer-html.ts:1068`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
  1063 |         if (task.working_directory) detailsHtml += '<div class="detail-row"><span class="detail-label">Directory</span><span class="detail-value">' + escapeHtml(task.working_directory) + '</span></div>';
  1064 |         if (task.result) detailsHtml += '<div class="detail-row"><span class="detail-label">Result</span><span class="detail-value">' + escapeHtml(task.result) + '</span></div>';
  1065 |         if (task.error_message) detailsHtml += '<div class="detail-row"><span class="detail-label">Error</span><span class="detail-value" style="color:var(--error)">' + escapeHtml(task.error_message) + '</span></div>';
  1066 |         if (task.group_id) detailsHtml += '<div class="detail-row"><span class="detail-label">Group ID</span><span class="detail-value">#' + task.group_id + '</span></div>';
  1067 | 
► 1068 |         card.innerHTML =
  1069 |           '<div class="card-top">' +
  1070 |             '<span class="card-id">#' + (task.task_id || '?') + '</span>' +
  1071 |             '<span class="card-desc">' + escapeHtml(task.description || 'Untitled task') + '</span>' +
  1072 |           '</div>' +
  1073 |           '<div class="card-meta">' + metaHtml + '</div>' +
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/board-viewer-html.ts:1091`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
  1086 |   }
  1087 | 
  1088 |   // ── Render Messages ──────────────────
  1089 |   function renderMessages(messages) {
  1090 |     if (messages.length === 0) {
► 1091 |       messageList.innerHTML = '<div class="no-messages">No messages yet</div>';
  1092 |       return;
  1093 |     }
  1094 | 
  1095 |     // Sort by most recent first, limit to 30
  1096 |     const sorted = [...messages].sort((a, b) => {
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/board-viewer-html.ts:1102`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
  1097 |       const tA = a.created_at || '';
  1098 |       const tB = b.created_at || '';
  1099 |       return tB.localeCompare(tA);
  1100 |     }).slice(0, 30);
  1101 | 
► 1102 |     messageList.innerHTML = '';
  1103 |     for (const msg of sorted) {
  1104 |       const item = document.createElement('div');
  1105 |       item.className = 'message-item';
  1106 | 
  1107 |       const typeClass = 'type-' + (msg.message_type || 'status');
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/board-viewer-html.ts:1110`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
  1105 |       item.className = 'message-item';
  1106 | 
  1107 |       const typeClass = 'type-' + (msg.message_type || 'status');
  1108 |       const typeLabel = (msg.message_type || 'status').toUpperCase();
  1109 | 
► 1110 |       item.innerHTML =
  1111 |         '<div class="msg-header">' +
  1112 |           '<span class="msg-from">' + escapeHtml(msg.from_agent || '?') + '</span>' +
  1113 |           '<span class="msg-arrow">→</span>' +
  1114 |           '<span class="msg-to">' + escapeHtml(msg.to_agent || '?') + '</span>' +
  1115 |           '<span class="msg-type ' + typeClass + '">' + typeLabel + '</span>' +
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/board-viewer-html.ts:1132`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
  1127 |       groupSection.style.display = 'none';
  1128 |       return;
  1129 |     }
  1130 | 
  1131 |     groupSection.style.display = '';
► 1132 |     groupSection.innerHTML = '<div class="group-section-header">Groups</div>';
  1133 | 
  1134 |     // Sort by most recently updated
  1135 |     const sorted = [...groups].sort((a, b) => {
  1136 |       const tA = a.updated_at || a.created_at || '';
  1137 |       const tB = b.updated_at || b.created_at || '';
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/board-viewer-html.ts:1149`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
  1144 | 
  1145 |       const total = group.total_tasks || group.task_count || 0;
  1146 |       const completed = group.completed_tasks || 0;
  1147 |       const pct = total > 0 ? Math.round((completed / total) * 100) : 0;
  1148 | 
► 1149 |       card.innerHTML =
  1150 |         '<div class="group-name">' + escapeHtml(group.group_name || group.name || 'Unnamed') + '</div>' +
  1151 |         '<div class="group-bar-wrap"><div class="group-bar" style="width:' + pct + '%"></div></div>' +
  1152 |         '<div class="group-stats">' +
  1153 |           '<span>' + completed + '/' + total + ' tasks</span>' +
  1154 |           '<span>' + pct + '%</span>' +
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/cleanup-viewer-html.ts:787`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   782 |   };
   783 | 
   784 |   // -- Render Results --
   785 |   function renderResults(data) {
   786 |     var container = document.getElementById('category-groups');
►  787 |     container.innerHTML = '';
   788 | 
   789 |     var order = ['temp', 'compiled', 'archives'];
   790 |     var labels = { temp: 'Temporary Files', compiled: 'Compiled / Build Artifacts', archives: 'Archives' };
   791 | 
   792 |     order.forEach(function(cat) {
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/cleanup-viewer-html.ts:801`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   796 | 
   797 |       var group = document.createElement('div');
   798 |       group.className = 'category-group';
   799 |       group.dataset.category = cat;
   800 | 
►  801 |       group.innerHTML =
   802 |         '<div class="category-header" onclick="toggleCategory(this.parentElement)">' +
   803 |           '<div class="category-header-left">' +
   804 |             '<span class="category-dot category-dot-' + cat + '"></span>' +
   805 |             '<span class="category-name">' + labels[cat] + '</span>' +
   806 |           '</div>' +
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/cleanup-viewer-html.ts:825`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   820 | 
   821 |       var fileListEl = group.querySelector('.file-list-rows');
   822 |       files.forEach(function(file) {
   823 |         var row = document.createElement('div');
   824 |         row.className = 'file-row';
►  825 |         row.innerHTML =
   826 |           '<input type="checkbox" data-path="' + escapeAttr(file.path) + '" onchange="toggleFileSelection(this)">' +
   827 |           '<span class="file-name">' + escapeHtml(file.name) + '</span>' +
   828 |           '<span class="file-path-col" title="' + escapeAttr(file.path) + '">' + escapeHtml(file.path) + '</span>' +
   829 |           (file.isDirectory ? '<span class="dir-badge">dir</span>' : '') +
   830 |           '<span class="file-size">' + file.sizeFormatted + '</span>' +
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/cleanup-viewer-html.ts:839`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   834 | 
   835 |       container.appendChild(group);
   836 |     });
   837 | 
   838 |     document.getElementById('results').style.display = 'block';
►  839 |     document.getElementById('results-summary').innerHTML =
   840 |       'Found <strong>' + data.totalFiles + '</strong> items totaling <strong>' + data.totalSizeFormatted + '</strong>';
   841 |     document.getElementById('btn-analyze').style.display = 'inline-flex';
   842 |     document.getElementById('btn-clear').style.display = 'inline-flex';
   843 |     document.getElementById('footer-bar').style.display = 'block';
   844 |     document.getElementById('scan-stats').style.display = 'block';
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/cleanup-viewer-html.ts:888`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   883 |   window.analyzeWithAI = function() {
   884 |     if (!scanData) return;
   885 |     var panel = document.getElementById('ai-panel');
   886 |     var body = document.getElementById('ai-body');
   887 |     panel.style.display = 'block';
►  888 |     body.innerHTML = '<span class="ai-cursor"></span>';
   889 | 
   890 |     var sampleFiles = {};
   891 |     Object.keys(scanData.results).forEach(function(cat) {
   892 |       sampleFiles[cat] = scanData.results[cat].slice(0, 10).map(function(f) {
   893 |         return { name: f.name, path: f.path, size: f.sizeFormatted, isDirectory: f.isDirectory };
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/cleanup-viewer-html.ts:924`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   919 |             if (payload === '[DONE]') return;
   920 |             try {
   921 |               var msg = JSON.parse(payload);
   922 |               if (msg.text) {
   923 |                 fullText += msg.text;
►  924 |                 body.innerHTML = escapeHtml(fullText) + '<span class="ai-cursor"></span>';
   925 |                 body.scrollTop = body.scrollHeight;
   926 |               } else if (msg.done && msg.result) {
   927 |                 fullText = msg.result;
   928 |                 body.innerHTML = escapeHtml(fullText);
   929 |               } else if (msg.error) {
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/cleanup-viewer-html.ts:928`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   923 |                 fullText += msg.text;
   924 |                 body.innerHTML = escapeHtml(fullText) + '<span class="ai-cursor"></span>';
   925 |                 body.scrollTop = body.scrollHeight;
   926 |               } else if (msg.done && msg.result) {
   927 |                 fullText = msg.result;
►  928 |                 body.innerHTML = escapeHtml(fullText);
   929 |               } else if (msg.error) {
   930 |                 body.innerHTML = '<span style="color:var(--error)">Error: ' + escapeHtml(msg.error) + '</span>';
   931 |               }
   932 |             } catch(e) {}
   933 |           });
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/cleanup-viewer-html.ts:930`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   925 |                 body.scrollTop = body.scrollHeight;
   926 |               } else if (msg.done && msg.result) {
   927 |                 fullText = msg.result;
   928 |                 body.innerHTML = escapeHtml(fullText);
   929 |               } else if (msg.error) {
►  930 |                 body.innerHTML = '<span style="color:var(--error)">Error: ' + escapeHtml(msg.error) + '</span>';
   931 |               }
   932 |             } catch(e) {}
   933 |           });
   934 |           read();
   935 |         });
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/cleanup-viewer-html.ts:940`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   935 |         });
   936 |       }
   937 |       read();
   938 |     })
   939 |     .catch(function(err) {
►  940 |       body.innerHTML = '<span style="color:var(--error)">Failed: ' + escapeHtml(err.message) + '</span>';
   941 |     });
   942 |   };
   943 | 
   944 |   // -- Delete --
   945 |   window.showDeleteModal = function() {
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/cleanup-viewer-html.ts:948`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   943 | 
   944 |   // -- Delete --
   945 |   window.showDeleteModal = function() {
   946 |     if (selectedFiles.size === 0) return;
   947 |     var list = document.getElementById('modal-file-list');
►  948 |     list.innerHTML = '';
   949 |     var totalSize = 0;
   950 |     var filesToDelete = [];
   951 | 
   952 |     Object.values(scanData.results).forEach(function(files) {
   953 |       files.forEach(function(f) {
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/cleanup-viewer-html.ts:961`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   956 |           totalSize += f.size;
   957 |         }
   958 |       });
   959 |     });
   960 | 
►  961 |     list.innerHTML = filesToDelete.map(function(f) {
   962 |       return escapeHtml(f.path) + '  (' + f.sizeFormatted + ')';
   963 |     }).join('<br>');
   964 | 
   965 |     document.getElementById('modal-summary').innerHTML =
   966 |       '<strong>' + filesToDelete.length + '</strong> items, <strong>' + formatSize(totalSize) + '</strong> will be freed.';
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/cleanup-viewer-html.ts:965`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   960 | 
   961 |     list.innerHTML = filesToDelete.map(function(f) {
   962 |       return escapeHtml(f.path) + '  (' + f.sizeFormatted + ')';
   963 |     }).join('<br>');
   964 | 
►  965 |     document.getElementById('modal-summary').innerHTML =
   966 |       '<strong>' + filesToDelete.length + '</strong> items, <strong>' + formatSize(totalSize) + '</strong> will be freed.';
   967 |     document.getElementById('deleteModal').classList.add('active');
   968 |   };
   969 | 
   970 |   window.closeModal = function() {
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/cleanup-viewer-html.ts:1050`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
  1045 |     fetch('http://localhost:' + PORT + '/history')
  1046 |     .then(function(r) { return r.json(); })
  1047 |     .then(function(entries) {
  1048 |       var list = document.getElementById('history-list');
  1049 |       if (entries.length === 0) {
► 1050 |         list.innerHTML = '<div class="history-item"><span class="history-item-path">No deletions recorded.</span></div>';
  1051 |         return;
  1052 |       }
  1053 |       list.innerHTML = entries.map(function(e) {
  1054 |         return '<div class="history-item">' +
  1055 |           '<span class="history-item-path">' + escapeHtml(e.path) + '</span>' +
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/cleanup-viewer-html.ts:1053`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
  1048 |       var list = document.getElementById('history-list');
  1049 |       if (entries.length === 0) {
  1050 |         list.innerHTML = '<div class="history-item"><span class="history-item-path">No deletions recorded.</span></div>';
  1051 |         return;
  1052 |       }
► 1053 |       list.innerHTML = entries.map(function(e) {
  1054 |         return '<div class="history-item">' +
  1055 |           '<span class="history-item-path">' + escapeHtml(e.path) + '</span>' +
  1056 |           '<span class="history-item-meta">' +
  1057 |             '<span>' + formatSize(e.size) + '</span>' +
  1058 |             '<span>' + formatDate(e.timestamp) + '</span>' +
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/cleanup-viewer-html.ts:1085`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
  1080 | 
  1081 |   // -- Helpers --
  1082 |   function showStatus(msg, loading) {
  1083 |     var el = document.getElementById('status');
  1084 |     el.style.display = 'block';
► 1085 |     el.innerHTML = (loading ? '<span class="spinner"></span>' : '') + escapeHtml(msg);
  1086 |   }
  1087 |   function hideStatus() { document.getElementById('status').style.display = 'none'; }
  1088 |   function hideResults() {
  1089 |     document.getElementById('results').style.display = 'none';
  1090 |     document.getElementById('footer-bar').style.display = 'none';
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/cleanup-viewer-html.ts:1100`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
  1095 |     scanData = null;
  1096 |     selectedFiles.clear();
  1097 |     hideResults();
  1098 |     hideStatus();
  1099 |     document.getElementById('btn-clear').style.display = 'none';
► 1100 |     document.getElementById('category-groups').innerHTML = '';
  1101 |   };
  1102 | 
  1103 |   function showToast(msg, type) {
  1104 |     var t = document.getElementById('toast');
  1105 |     t.textContent = msg;
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/completion-report-html.ts:838`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   833 |     // Title
   834 |     document.getElementById('titleText').textContent = report.title;
   835 | 
   836 |     // Header stats
   837 |     const statsEl = document.getElementById('headerStats');
►  838 |     statsEl.innerHTML =
   839 |       '<span class="stat-files">' + report.files.length + ' file' + (report.files.length !== 1 ? 's' : '') + '</span>' +
   840 |       '<span class="stat-add">+' + report.totalAdditions + '</span>' +
   841 |       '<span class="stat-del">-' + report.totalDeletions + '</span>';
   842 | 
   843 |     // Task overview cards
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/completion-report-html.ts:849`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   844 |     renderOverviewCards();
   845 | 
   846 |     // Summary section
   847 |     if (report.summary && report.summary.trim()) {
   848 |       document.getElementById('summarySection').style.display = 'block';
►  849 |       document.getElementById('summaryContent').innerHTML = renderMarkdownWithTables(report.summary);
   850 |     }
   851 | 
   852 |     // Task section
   853 |     if (report.taskMarkdown && report.taskMarkdown.trim()) {
   854 |       document.getElementById('taskSection').style.display = 'block';
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/completion-report-html.ts:867`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   862 |     updateRollbackAllBtn();
   863 |   }
   864 | 
   865 |   function renderOverviewCards() {
   866 |     const el = document.getElementById('taskOverview');
►  867 |     el.innerHTML =
   868 |       '<div class="task-stat files"><div class="value">' + report.files.length + '</div><div class="label">Files Changed</div></div>' +
   869 |       '<div class="task-stat additions"><div class="value">+' + report.totalAdditions + '</div><div class="label">Additions</div></div>' +
   870 |       '<div class="task-stat deletions"><div class="value">-' + report.totalDeletions + '</div><div class="label">Deletions</div></div>';
   871 |   }
   872 | 
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/completion-report-html.ts:918`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   913 |           return '<li class="task-done">✓ ' + text + '</li>';
   914 |         }
   915 |         return '<li class="task-pending">☐ ' + text + '</li>';
   916 |       }
   917 |     );
►  918 |     document.getElementById('taskContent').innerHTML = html;
   919 |   }
   920 | 
   921 |   // ── File Rendering ────────────────────────────
   922 |   function renderFiles() {
   923 |     const container = document.getElementById('filesList');
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/completion-report-html.ts:924`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   919 |   }
   920 | 
   921 |   // ── File Rendering ────────────────────────────
   922 |   function renderFiles() {
   923 |     const container = document.getElementById('filesList');
►  924 |     container.innerHTML = '';
   925 | 
   926 |     report.files.forEach(function(file, idx) {
   927 |       const card = document.createElement('div');
   928 |       card.className = 'file-card';
   929 |       card.id = 'file-' + idx;
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/completion-report-html.ts:944`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   939 |       // Header
   940 |       const header = document.createElement('div');
   941 |       header.className = 'file-header';
   942 |       header.onclick = function() { toggleFile(idx); };
   943 | 
►  944 |       header.innerHTML =
   945 |         '<span class="file-chevron">▶</span>' +
   946 |         '<span class="file-status ' + file.status + '">' + file.status + '</span>' +
   947 |         '<span class="file-path"><span class="dir">' + escapeHtml(dir) + '</span><span class="name">' + escapeHtml(name) + '</span></span>' +
   948 |         '<span class="file-stats"><span class="add">+' + file.additions + '</span><span class="del">-' + file.deletions + '</span></span>' +
   949 |         '<div class="file-actions">' +
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/completion-report-html.ts:961`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   956 |       card.appendChild(header);
   957 | 
   958 |       // Diff
   959 |       const diffEl = document.createElement('div');
   960 |       diffEl.className = 'file-diff';
►  961 |       diffEl.innerHTML = renderDiff(file.diff);
   962 |       card.appendChild(diffEl);
   963 | 
   964 |       container.appendChild(card);
   965 |     });
   966 |   }
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/completion-report-html.ts:1091`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
  1086 |     pendingRollback = { files: files, type: 'all' };
  1087 |     showRollbackModal(files);
  1088 |   };
  1089 | 
  1090 |   function showRollbackModal(files) {
► 1091 |     document.getElementById('modalFileList').innerHTML = files.map(function(f) {
  1092 |       return escapeHtml(f);
  1093 |     }).join('<br>');
  1094 |     document.getElementById('modalTitle').textContent =
  1095 |       files.length === 1 ? 'Rollback File' : 'Rollback ' + files.length + ' Files';
  1096 |     document.getElementById('rollbackModal').classList.add('active');
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/completion-report-html.ts:1168`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
  1163 |   }
  1164 | 
  1165 |   function showDoneState() {
  1166 |     var banner = document.createElement('div');
  1167 |     banner.className = 'done-banner';
► 1168 |     banner.innerHTML = '<div class="done-icon">\u2713</div>' +
  1169 |       '<div class="done-content"><div class="done-text">Report Complete</div>' +
  1170 |       '<div class="done-sub">You can close this tab.</div></div>' +
  1171 |       '<div class="done-actions">' +
  1172 |       '<button class="icon-btn" onclick="copyReport()" title="Copy to clipboard"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="9" y="9" width="13" height="13" rx="2" ry="2"/><path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"/></svg></button>' +
  1173 |       '</div>';
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/file-viewer-html.ts:865`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   860 |     return html;
   861 |   }
   862 | 
   863 |   function generateLineNums(content, container) {
   864 |     var html = renderLineNumberHtml(getLineCount(content));
►  865 |     if (container.innerHTML !== html) container.innerHTML = html;
   866 |   }
   867 | 
   868 |   function updateGutter(content) {
   869 |     var html = renderLineNumberHtml(getLineCount(content));
   870 |     if (gutter.innerHTML !== html) gutter.innerHTML = html;
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/file-viewer-html.ts:870`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   865 |     if (container.innerHTML !== html) container.innerHTML = html;
   866 |   }
   867 | 
   868 |   function updateGutter(content) {
   869 |     var html = renderLineNumberHtml(getLineCount(content));
►  870 |     if (gutter.innerHTML !== html) gutter.innerHTML = html;
   871 |   }
   872 | 
   873 |   var lastHighlightedContent = null;
   874 | 
   875 |   function highlightCode() {
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/file-viewer-html.ts:885`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   880 |       if (lang) {
   881 |         result = hljs.highlight(currentContent, { language: lang });
   882 |       } else {
   883 |         result = hljs.highlightAuto(currentContent);
   884 |       }
►  885 |       codeBlock.innerHTML = result.value;
   886 |       codeBlock.className = (lang ? 'language-' + lang + ' ' : '') + 'hljs';
   887 |     } else {
   888 |       codeBlock.textContent = currentContent;
   889 |     }
   890 |     lastHighlightedContent = currentContent;
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/file-viewer-html.ts:901`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   896 | 
   897 |   function renderMarkdown() {
   898 |     if (!IS_MARKDOWN) return;
   899 |     if (typeof marked !== 'undefined' && marked && typeof marked.parse === 'function') {
   900 |       marked.setOptions({ gfm: true, breaks: true });
►  901 |       markdownRendered.innerHTML = marked.parse(preprocessMarkdown(currentContent));
   902 |     } else {
   903 |       markdownRendered.textContent = currentContent;
   904 |     }
   905 |   }
   906 | 
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### Directory Traversal Pattern

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/persist-theme.ts:8`  
**Rule:** [`SAST-PT-002`](https://owasp.org/www-community/attacks/Path_Traversal)  
**CWE:** [CWE-23](https://cwe.mitre.org/data/definitions/23.html)  
**OWASP:** [A01:2021](https://owasp.org/Top10/A01_2021-Broken_Access_Control/)  

> Path containing ../ patterns used in file operations may allow directory traversal attacks

**Source Context:**
```
     3 | 
     4 | import { readFileSync, writeFileSync } from "fs";
     5 | import { dirname, resolve } from "path";
     6 | import { fileURLToPath } from "url";
     7 | 
►    8 | const DEFAULT_SETTINGS_PATH = resolve(dirname(fileURLToPath(import.meta.url)), "../../settings.json");
     9 | 
    10 | /**
    11 |  * Write the chosen theme name into settings.json so it survives restarts.
    12 |  * Silently catches errors — theme persistence is non-critical.
    13 |  */
```

**Remediation:**
Reject paths containing "../". Use path.resolve() and verify the resolved path is within allowed directories

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/plan-viewer-html.ts:940`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   935 |           '<button class="delete-btn" onclick="deleteItem(this)" title="Delete item"><svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg></button>' +
   936 |           '</li>';
   937 |       }
   938 |     );
   939 | 
►  940 |     container.innerHTML = html;
   941 |     enhanceStructuredPlan(container);
   942 |     setupDragAndDrop();
   943 |   }
   944 | 
   945 |   // ── Post-process rendered HTML for structured plan format ──
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/plan-viewer-html.ts:1019`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
  1014 |             var codeEl = p.querySelector('code');
  1015 |             var filePath = codeEl ? codeEl.textContent : '';
  1016 | 
  1017 |             var indicator = document.createElement('div');
  1018 |             indicator.className = 'file-indicator';
► 1019 |             indicator.innerHTML =
  1020 |               '<span class="file-action ' + actionType + '">' + actionLabel + '</span>' +
  1021 |               (filePath ? '<span class="file-path">' + escapeHtml(filePath) + '</span>' : '');
  1022 | 
  1023 |             p.parentNode.insertBefore(indicator, p);
  1024 |             p.style.display = 'none';
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/plan-viewer-html.ts:1154`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
  1149 |     if (buffer.length > 0) {
  1150 |       html += '<div class="markdown-body">' + marked.parse(buffer.join('\\n')) + '</div>';
  1151 |     }
  1152 | 
  1153 |     questionCount = qNum;
► 1154 |     container.innerHTML = html;
  1155 |     updateProgress();
  1156 |   }
  1157 | 
  1158 |   function isQuestionLine(line) {
  1159 |     const trimmed = line.trim();
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/plan-viewer-html.ts:1522`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
  1517 |         var sub = MODE === 'questions' ? 'Your answers have been sent to the agent.' : 'The agent will now proceed with implementation.';
  1518 | 
  1519 |         // Insert approved banner after header
  1520 |         var banner = document.createElement('div');
  1521 |         banner.className = 'approved-banner';
► 1522 |         banner.innerHTML = '<div class="approved-icon">✓</div>' +
  1523 |           '<div class="approved-content"><div class="approved-text">' + label + '</div>' +
  1524 |           '<div class="approved-sub">' + sub + '</div></div>' +
  1525 |           '<div class="approved-actions">' +
  1526 |           '<button class="icon-btn" onclick="copyToClipboard()" title="Copy to clipboard"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="9" y="9" width="13" height="13" rx="2" ry="2"/><path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"/></svg></button>' +
  1527 |           '</div>';
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/plan-viewer-html.ts:1548`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
  1543 |         if (currentView === 'raw') {
  1544 |           setView('rendered');
  1545 |         }
  1546 |       } else {
  1547 |         // Closed/declined — show simple close message
► 1548 |         document.body.innerHTML = '<div style="display:flex;align-items:center;justify-content:center;height:100vh;color:var(--text-muted);font-family:var(--font);">' +
  1549 |           '<div style="text-align:center"><p style="font-size:20px;margin-bottom:8px;">Closed</p>' +
  1550 |           '<p style="color:var(--text-dim);">You can close this tab.</p></div></div>';
  1551 |       }
  1552 |     }).catch(() => {
  1553 |       showToast('Failed to send result');
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/reports-viewer-html.ts:488`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   483 |     document.getElementById('homeView').style.display = 'none';
   484 |     document.getElementById('tableView').classList.add('active');
   485 |     document.getElementById('tableTitle').textContent = labels[category];
   486 |     document.getElementById('tableCount').textContent = rows.length + ' result' + (rows.length === 1 ? '' : 's');
   487 |     const tbody = document.getElementById('tableBody');
►  488 |     tbody.innerHTML = rows.length ? rows.map((entry) =>
   489 |       '<tr data-clickable="true" onclick="openFromTable(\\'' + entry.id + '\\')">' +
   490 |         '<td><div class="row-title">' + escapeHtml(entry.title) + '</div><span class="pill ' + entry.category + '">' + escapeHtml(entry.category) + '</span></td>' +
   491 |         '<td>' + escapeHtml(entry.summary || '—') + '</td>' +
   492 |         '<td>' + escapeHtml(entry.sourceLabel || entry.viewerLabel || 'Viewer') + '</td>' +
   493 |         '<td>' + escapeHtml(fmtDate(entry.updatedAt)) + '</td>' +
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/reports-viewer-html.ts:512`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   507 |   };
   508 | 
   509 |   function renderChips() {
   510 |     const wrap = document.getElementById('chips');
   511 |     const all = ['all'].concat(categories);
►  512 |     wrap.innerHTML = all.map((cat) =>
   513 |       '<button class="chip ' + (activeCategory === cat ? 'active' : '') + '" onclick="setCategory(\\'' + cat + '\\')">' +
   514 |       (cat === 'all' ? 'All Reports' : labels[cat]) +
   515 |       '</button>'
   516 |     ).join('');
   517 |   }
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/reports-viewer-html.ts:523`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   518 | 
   519 |   function renderStats() {
   520 |     const total = filtered().length;
   521 |     const stats = document.getElementById('stats');
   522 |     const counts = Object.fromEntries(categories.map((c) => [c, byCategory(c).length]));
►  523 |     stats.innerHTML = [
   524 |       ['Visible Reports', total],
   525 |       ['Plans', counts.plan],
   526 |       ['Questions', counts.questions],
   527 |       ['Specs', counts.spec],
   528 |       ['Completion', counts.completion],
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/reports-viewer-html.ts:536`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   531 |     ).join('');
   532 |   }
   533 | 
   534 |   function renderSections() {
   535 |     const sections = document.getElementById('sections');
►  536 |     sections.innerHTML = categories.map((category) => {
   537 |       const rows = byCategory(category);
   538 |       const recent = rows.slice(0, 5);
   539 |       return '<div class="section">' +
   540 |         '<div class="section-header">' +
   541 |           '<div class="section-title">' + labels[category] + '</div>' +
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/research-viewer-html.ts:480`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   475 |   const active = sessions.filter(s => ['researching', 'implementing', 'understanding', 'planning'].includes(s.status)).length;
   476 |   const complete = sessions.filter(s => s.status === 'complete').length;
   477 |   const paused = sessions.filter(s => s.status === 'paused').length;
   478 |   const totalIterations = sessions.reduce((sum, s) => sum + (s.iterationCount || 0), 0);
   479 | 
►  480 |   document.getElementById('stats').innerHTML = [
   481 |     { value: total, label: 'Sessions' },
   482 |     { value: active, label: 'Active' },
   483 |     { value: complete, label: 'Complete' },
   484 |     { value: paused, label: 'Paused' },
   485 |     { value: totalIterations, label: 'Iterations' },
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/research-viewer-html.ts:493`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   488 | 
   489 | function renderFilters() {
   490 |   const statuses = ['all', 'researching', 'implementing', 'complete', 'paused'];
   491 |   const counts = {};
   492 |   statuses.forEach(s => { counts[s] = s === 'all' ? sessions.length : sessions.filter(x => x.status === s).length; });
►  493 |   document.getElementById('filters').innerHTML = statuses.map(s =>
   494 |     '<button class="chip' + (activeFilter === s ? ' active' : '') + '" data-filter="' + s + '">' +
   495 |     (s === 'all' ? 'All' : statusLabel(s)) + ' (' + counts[s] + ')</button>'
   496 |   ).join('');
   497 | }
   498 | 
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/research-viewer-html.ts:515`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   510 | }
   511 | 
   512 | function renderSessions() {
   513 |   const filtered = getFiltered();
   514 |   if (filtered.length === 0) {
►  515 |     document.getElementById('sessions').innerHTML = '<div class="empty-state"><h2>No sessions found</h2><p>Try adjusting your search or filter.</p></div>';
   516 |     return;
   517 |   }
   518 |   document.getElementById('sessions').innerHTML = filtered.map(s => {
   519 |     const delta = s.final != null && s.baseline != null ? (s.final - s.baseline) : null;
   520 |     const deltaStr = delta != null ? (delta >= 0 ? '+' + delta.toFixed(2) : delta.toFixed(2)) : '';
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/research-viewer-html.ts:518`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   513 |   const filtered = getFiltered();
   514 |   if (filtered.length === 0) {
   515 |     document.getElementById('sessions').innerHTML = '<div class="empty-state"><h2>No sessions found</h2><p>Try adjusting your search or filter.</p></div>';
   516 |     return;
   517 |   }
►  518 |   document.getElementById('sessions').innerHTML = filtered.map(s => {
   519 |     const delta = s.final != null && s.baseline != null ? (s.final - s.baseline) : null;
   520 |     const deltaStr = delta != null ? (delta >= 0 ? '+' + delta.toFixed(2) : delta.toFixed(2)) : '';
   521 |     const deltaClass = delta != null ? (delta >= 0 === (s.metricDirection === 'higher') ? 'delta-positive' : 'delta-negative') : '';
   522 | 
   523 |     return '<div class="session-card" data-id="' + s.id + '">' +
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/research-viewer-html.ts:660`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   655 |     const cmd = '/autoresearch --resume ' + s.id;
   656 |     html += '<div class="resume-box"><code>' + escapeHtmlJS(cmd) + '</code>' +
   657 |       '<button class="copy-btn" onclick="navigator.clipboard.writeText(\\'' + cmd.replace(/'/g, "\\\\'") + '\\');this.textContent=\\'Copied!\\';setTimeout(()=>this.textContent=\\'Copy\\',2000)">Copy</button></div>';
   658 |   }
   659 | 
►  660 |   document.getElementById('detail-view').innerHTML = html;
   661 | }
   662 | 
   663 | // Event listeners
   664 | document.getElementById('search').addEventListener('input', (e) => {
   665 |   searchQuery = e.target.value;
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### Regular Expression Denial of Service (ReDoS)

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/secure-engine.ts:563`  
**Rule:** [`SAST-ADV-003`](https://owasp.org/Top10/A09_2021-Security_Logging_and_Monitoring_Failures/)  
**CWE:** [CWE-1333](https://cwe.mitre.org/data/definitions/1333.html)  
**OWASP:** [A06:2021](https://owasp.org/Top10/A06_2021-Vulnerable_and_Outdated_Components/)  

> Unsanitized input used in regular expression constructors can cause catastrophic backtracking and denial of service

**Source Context:**
```
   558 | 	// ── Check for env vars with AI keys in .env files ────────
   559 | 	if (filePath.includes(".env")) {
   560 | 		for (const svc of AI_SERVICE_PATTERNS) {
   561 | 			if (svc.envPattern.test(content)) {
   562 | 				// Check if value is actually present (not just a placeholder)
►  563 | 				const envMatch = content.match(new RegExp(`${svc.envPattern.source}\\s*=\\s*(.+)`, "im"));
   564 | 				if (envMatch && envMatch[1]) {
   565 | 					const value = envMatch[1].trim();
   566 | 					// Skip obvious placeholders
   567 | 					if (value && !value.match(/^(your[_-]?|xxx|placeholder|changeme|TODO|FIXME|<|"|')/i) && value.length > 10) {
   568 | 						// Check if .env is in .gitignore
```

**Remediation:**
Never construct regex from user input. Use string methods or escape special characters with escape-string-regexp

---

#### Regular Expression Denial of Service (ReDoS)

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/secure-installer.ts:501`  
**Rule:** [`SAST-ADV-003`](https://owasp.org/Top10/A09_2021-Security_Logging_and_Monitoring_Failures/)  
**CWE:** [CWE-1333](https://cwe.mitre.org/data/definitions/1333.html)  
**OWASP:** [A06:2021](https://owasp.org/Top10/A06_2021-Vulnerable_and_Outdated_Components/)  

> Unsanitized input used in regular expression constructors can cause catastrophic backtracking and denial of service

**Source Context:**
```
   496 | # ═══════════════════════════════════════════════════════════════════
   497 | # Prompt Injection Patterns
   498 | # ═══════════════════════════════════════════════════════════════════
   499 | 
   500 | INJECTION_PATTERNS = [
►  501 |     (re.compile(r"ignore\\s+(all\\s+)?(previous|prior|above)\\s+(instructions?|rules?|prompts?)", re.I), "Instruction override"),
   502 |     (re.compile(r"disregard\\s+(all\\s+)?(previous|prior)\\s+(instructions?|rules?)", re.I), "Instruction disregard"),
   503 |     (re.compile(r"forget\\s+(all\\s+)?(previous|prior)\\s+(instructions?|context)", re.I), "Context reset"),
   504 |     (re.compile(r"new\\s+system\\s+prompt", re.I), "System prompt injection"),
   505 |     (re.compile(r"override\\s+(your|the|all)\\s+(rules?|instructions?|safety)", re.I), "Safety override"),
   506 |     (re.compile(r"(you\\s+are|act\\s+as|pretend)\\s+(now\\s+)?(a\\s+)?(different|unrestricted|jailbroken)", re.I), "Role hijacking"),
```

**Remediation:**
Never construct regex from user input. Use re.escape() or fixed patterns

---

#### Regular Expression Denial of Service (ReDoS)

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/secure-installer.ts:502`  
**Rule:** [`SAST-ADV-003`](https://owasp.org/Top10/A09_2021-Security_Logging_and_Monitoring_Failures/)  
**CWE:** [CWE-1333](https://cwe.mitre.org/data/definitions/1333.html)  
**OWASP:** [A06:2021](https://owasp.org/Top10/A06_2021-Vulnerable_and_Outdated_Components/)  

> Unsanitized input used in regular expression constructors can cause catastrophic backtracking and denial of service

**Source Context:**
```
   497 | # Prompt Injection Patterns
   498 | # ═══════════════════════════════════════════════════════════════════
   499 | 
   500 | INJECTION_PATTERNS = [
   501 |     (re.compile(r"ignore\\s+(all\\s+)?(previous|prior|above)\\s+(instructions?|rules?|prompts?)", re.I), "Instruction override"),
►  502 |     (re.compile(r"disregard\\s+(all\\s+)?(previous|prior)\\s+(instructions?|rules?)", re.I), "Instruction disregard"),
   503 |     (re.compile(r"forget\\s+(all\\s+)?(previous|prior)\\s+(instructions?|context)", re.I), "Context reset"),
   504 |     (re.compile(r"new\\s+system\\s+prompt", re.I), "System prompt injection"),
   505 |     (re.compile(r"override\\s+(your|the|all)\\s+(rules?|instructions?|safety)", re.I), "Safety override"),
   506 |     (re.compile(r"(you\\s+are|act\\s+as|pretend)\\s+(now\\s+)?(a\\s+)?(different|unrestricted|jailbroken)", re.I), "Role hijacking"),
   507 |     (re.compile(r"DAN\\s+(mode|prompt)|do\\s+anything\\s+now", re.I), "DAN jailbreak"),
```

**Remediation:**
Never construct regex from user input. Use re.escape() or fixed patterns

---

#### Regular Expression Denial of Service (ReDoS)

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/secure-installer.ts:503`  
**Rule:** [`SAST-ADV-003`](https://owasp.org/Top10/A09_2021-Security_Logging_and_Monitoring_Failures/)  
**CWE:** [CWE-1333](https://cwe.mitre.org/data/definitions/1333.html)  
**OWASP:** [A06:2021](https://owasp.org/Top10/A06_2021-Vulnerable_and_Outdated_Components/)  

> Unsanitized input used in regular expression constructors can cause catastrophic backtracking and denial of service

**Source Context:**
```
   498 | # ═══════════════════════════════════════════════════════════════════
   499 | 
   500 | INJECTION_PATTERNS = [
   501 |     (re.compile(r"ignore\\s+(all\\s+)?(previous|prior|above)\\s+(instructions?|rules?|prompts?)", re.I), "Instruction override"),
   502 |     (re.compile(r"disregard\\s+(all\\s+)?(previous|prior)\\s+(instructions?|rules?)", re.I), "Instruction disregard"),
►  503 |     (re.compile(r"forget\\s+(all\\s+)?(previous|prior)\\s+(instructions?|context)", re.I), "Context reset"),
   504 |     (re.compile(r"new\\s+system\\s+prompt", re.I), "System prompt injection"),
   505 |     (re.compile(r"override\\s+(your|the|all)\\s+(rules?|instructions?|safety)", re.I), "Safety override"),
   506 |     (re.compile(r"(you\\s+are|act\\s+as|pretend)\\s+(now\\s+)?(a\\s+)?(different|unrestricted|jailbroken)", re.I), "Role hijacking"),
   507 |     (re.compile(r"DAN\\s+(mode|prompt)|do\\s+anything\\s+now", re.I), "DAN jailbreak"),
   508 |     (re.compile(r"(dump|reveal|show|output)\\s+(your|the)\\s+(system\\s+prompt|instructions?|api\\s+keys?|secrets?)", re.I), "Data extraction"),
```

**Remediation:**
Never construct regex from user input. Use re.escape() or fixed patterns

---

#### Regular Expression Denial of Service (ReDoS)

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/secure-installer.ts:504`  
**Rule:** [`SAST-ADV-003`](https://owasp.org/Top10/A09_2021-Security_Logging_and_Monitoring_Failures/)  
**CWE:** [CWE-1333](https://cwe.mitre.org/data/definitions/1333.html)  
**OWASP:** [A06:2021](https://owasp.org/Top10/A06_2021-Vulnerable_and_Outdated_Components/)  

> Unsanitized input used in regular expression constructors can cause catastrophic backtracking and denial of service

**Source Context:**
```
   499 | 
   500 | INJECTION_PATTERNS = [
   501 |     (re.compile(r"ignore\\s+(all\\s+)?(previous|prior|above)\\s+(instructions?|rules?|prompts?)", re.I), "Instruction override"),
   502 |     (re.compile(r"disregard\\s+(all\\s+)?(previous|prior)\\s+(instructions?|rules?)", re.I), "Instruction disregard"),
   503 |     (re.compile(r"forget\\s+(all\\s+)?(previous|prior)\\s+(instructions?|context)", re.I), "Context reset"),
►  504 |     (re.compile(r"new\\s+system\\s+prompt", re.I), "System prompt injection"),
   505 |     (re.compile(r"override\\s+(your|the|all)\\s+(rules?|instructions?|safety)", re.I), "Safety override"),
   506 |     (re.compile(r"(you\\s+are|act\\s+as|pretend)\\s+(now\\s+)?(a\\s+)?(different|unrestricted|jailbroken)", re.I), "Role hijacking"),
   507 |     (re.compile(r"DAN\\s+(mode|prompt)|do\\s+anything\\s+now", re.I), "DAN jailbreak"),
   508 |     (re.compile(r"(dump|reveal|show|output)\\s+(your|the)\\s+(system\\s+prompt|instructions?|api\\s+keys?|secrets?)", re.I), "Data extraction"),
   509 |     (re.compile(r"(upload|send|post|exfiltrate)\\s+.*(to|at)\\s+https?://", re.I), "Data exfiltration"),
```

**Remediation:**
Never construct regex from user input. Use re.escape() or fixed patterns

---

#### Regular Expression Denial of Service (ReDoS)

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/secure-installer.ts:505`  
**Rule:** [`SAST-ADV-003`](https://owasp.org/Top10/A09_2021-Security_Logging_and_Monitoring_Failures/)  
**CWE:** [CWE-1333](https://cwe.mitre.org/data/definitions/1333.html)  
**OWASP:** [A06:2021](https://owasp.org/Top10/A06_2021-Vulnerable_and_Outdated_Components/)  

> Unsanitized input used in regular expression constructors can cause catastrophic backtracking and denial of service

**Source Context:**
```
   500 | INJECTION_PATTERNS = [
   501 |     (re.compile(r"ignore\\s+(all\\s+)?(previous|prior|above)\\s+(instructions?|rules?|prompts?)", re.I), "Instruction override"),
   502 |     (re.compile(r"disregard\\s+(all\\s+)?(previous|prior)\\s+(instructions?|rules?)", re.I), "Instruction disregard"),
   503 |     (re.compile(r"forget\\s+(all\\s+)?(previous|prior)\\s+(instructions?|context)", re.I), "Context reset"),
   504 |     (re.compile(r"new\\s+system\\s+prompt", re.I), "System prompt injection"),
►  505 |     (re.compile(r"override\\s+(your|the|all)\\s+(rules?|instructions?|safety)", re.I), "Safety override"),
   506 |     (re.compile(r"(you\\s+are|act\\s+as|pretend)\\s+(now\\s+)?(a\\s+)?(different|unrestricted|jailbroken)", re.I), "Role hijacking"),
   507 |     (re.compile(r"DAN\\s+(mode|prompt)|do\\s+anything\\s+now", re.I), "DAN jailbreak"),
   508 |     (re.compile(r"(dump|reveal|show|output)\\s+(your|the)\\s+(system\\s+prompt|instructions?|api\\s+keys?|secrets?)", re.I), "Data extraction"),
   509 |     (re.compile(r"(upload|send|post|exfiltrate)\\s+.*(to|at)\\s+https?://", re.I), "Data exfiltration"),
   510 | ]
```

**Remediation:**
Never construct regex from user input. Use re.escape() or fixed patterns

---

#### Regular Expression Denial of Service (ReDoS)

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/secure-installer.ts:506`  
**Rule:** [`SAST-ADV-003`](https://owasp.org/Top10/A09_2021-Security_Logging_and_Monitoring_Failures/)  
**CWE:** [CWE-1333](https://cwe.mitre.org/data/definitions/1333.html)  
**OWASP:** [A06:2021](https://owasp.org/Top10/A06_2021-Vulnerable_and_Outdated_Components/)  

> Unsanitized input used in regular expression constructors can cause catastrophic backtracking and denial of service

**Source Context:**
```
   501 |     (re.compile(r"ignore\\s+(all\\s+)?(previous|prior|above)\\s+(instructions?|rules?|prompts?)", re.I), "Instruction override"),
   502 |     (re.compile(r"disregard\\s+(all\\s+)?(previous|prior)\\s+(instructions?|rules?)", re.I), "Instruction disregard"),
   503 |     (re.compile(r"forget\\s+(all\\s+)?(previous|prior)\\s+(instructions?|context)", re.I), "Context reset"),
   504 |     (re.compile(r"new\\s+system\\s+prompt", re.I), "System prompt injection"),
   505 |     (re.compile(r"override\\s+(your|the|all)\\s+(rules?|instructions?|safety)", re.I), "Safety override"),
►  506 |     (re.compile(r"(you\\s+are|act\\s+as|pretend)\\s+(now\\s+)?(a\\s+)?(different|unrestricted|jailbroken)", re.I), "Role hijacking"),
   507 |     (re.compile(r"DAN\\s+(mode|prompt)|do\\s+anything\\s+now", re.I), "DAN jailbreak"),
   508 |     (re.compile(r"(dump|reveal|show|output)\\s+(your|the)\\s+(system\\s+prompt|instructions?|api\\s+keys?|secrets?)", re.I), "Data extraction"),
   509 |     (re.compile(r"(upload|send|post|exfiltrate)\\s+.*(to|at)\\s+https?://", re.I), "Data exfiltration"),
   510 | ]
   511 | 
```

**Remediation:**
Never construct regex from user input. Use re.escape() or fixed patterns

---

#### Regular Expression Denial of Service (ReDoS)

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/secure-installer.ts:507`  
**Rule:** [`SAST-ADV-003`](https://owasp.org/Top10/A09_2021-Security_Logging_and_Monitoring_Failures/)  
**CWE:** [CWE-1333](https://cwe.mitre.org/data/definitions/1333.html)  
**OWASP:** [A06:2021](https://owasp.org/Top10/A06_2021-Vulnerable_and_Outdated_Components/)  

> Unsanitized input used in regular expression constructors can cause catastrophic backtracking and denial of service

**Source Context:**
```
   502 |     (re.compile(r"disregard\\s+(all\\s+)?(previous|prior)\\s+(instructions?|rules?)", re.I), "Instruction disregard"),
   503 |     (re.compile(r"forget\\s+(all\\s+)?(previous|prior)\\s+(instructions?|context)", re.I), "Context reset"),
   504 |     (re.compile(r"new\\s+system\\s+prompt", re.I), "System prompt injection"),
   505 |     (re.compile(r"override\\s+(your|the|all)\\s+(rules?|instructions?|safety)", re.I), "Safety override"),
   506 |     (re.compile(r"(you\\s+are|act\\s+as|pretend)\\s+(now\\s+)?(a\\s+)?(different|unrestricted|jailbroken)", re.I), "Role hijacking"),
►  507 |     (re.compile(r"DAN\\s+(mode|prompt)|do\\s+anything\\s+now", re.I), "DAN jailbreak"),
   508 |     (re.compile(r"(dump|reveal|show|output)\\s+(your|the)\\s+(system\\s+prompt|instructions?|api\\s+keys?|secrets?)", re.I), "Data extraction"),
   509 |     (re.compile(r"(upload|send|post|exfiltrate)\\s+.*(to|at)\\s+https?://", re.I), "Data exfiltration"),
   510 | ]
   511 | 
   512 | SENSITIVE_PATTERNS = [
```

**Remediation:**
Never construct regex from user input. Use re.escape() or fixed patterns

---

#### Regular Expression Denial of Service (ReDoS)

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/secure-installer.ts:508`  
**Rule:** [`SAST-ADV-003`](https://owasp.org/Top10/A09_2021-Security_Logging_and_Monitoring_Failures/)  
**CWE:** [CWE-1333](https://cwe.mitre.org/data/definitions/1333.html)  
**OWASP:** [A06:2021](https://owasp.org/Top10/A06_2021-Vulnerable_and_Outdated_Components/)  

> Unsanitized input used in regular expression constructors can cause catastrophic backtracking and denial of service

**Source Context:**
```
   503 |     (re.compile(r"forget\\s+(all\\s+)?(previous|prior)\\s+(instructions?|context)", re.I), "Context reset"),
   504 |     (re.compile(r"new\\s+system\\s+prompt", re.I), "System prompt injection"),
   505 |     (re.compile(r"override\\s+(your|the|all)\\s+(rules?|instructions?|safety)", re.I), "Safety override"),
   506 |     (re.compile(r"(you\\s+are|act\\s+as|pretend)\\s+(now\\s+)?(a\\s+)?(different|unrestricted|jailbroken)", re.I), "Role hijacking"),
   507 |     (re.compile(r"DAN\\s+(mode|prompt)|do\\s+anything\\s+now", re.I), "DAN jailbreak"),
►  508 |     (re.compile(r"(dump|reveal|show|output)\\s+(your|the)\\s+(system\\s+prompt|instructions?|api\\s+keys?|secrets?)", re.I), "Data extraction"),
   509 |     (re.compile(r"(upload|send|post|exfiltrate)\\s+.*(to|at)\\s+https?://", re.I), "Data exfiltration"),
   510 | ]
   511 | 
   512 | SENSITIVE_PATTERNS = [
   513 |     (re.compile(r"sk-[a-zA-Z0-9]{20,}"), "sk-[REDACTED]", "OpenAI API key"),
```

**Remediation:**
Never construct regex from user input. Use re.escape() or fixed patterns

---

#### Regular Expression Denial of Service (ReDoS)

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/secure-installer.ts:509`  
**Rule:** [`SAST-ADV-003`](https://owasp.org/Top10/A09_2021-Security_Logging_and_Monitoring_Failures/)  
**CWE:** [CWE-1333](https://cwe.mitre.org/data/definitions/1333.html)  
**OWASP:** [A06:2021](https://owasp.org/Top10/A06_2021-Vulnerable_and_Outdated_Components/)  

> Unsanitized input used in regular expression constructors can cause catastrophic backtracking and denial of service

**Source Context:**
```
   504 |     (re.compile(r"new\\s+system\\s+prompt", re.I), "System prompt injection"),
   505 |     (re.compile(r"override\\s+(your|the|all)\\s+(rules?|instructions?|safety)", re.I), "Safety override"),
   506 |     (re.compile(r"(you\\s+are|act\\s+as|pretend)\\s+(now\\s+)?(a\\s+)?(different|unrestricted|jailbroken)", re.I), "Role hijacking"),
   507 |     (re.compile(r"DAN\\s+(mode|prompt)|do\\s+anything\\s+now", re.I), "DAN jailbreak"),
   508 |     (re.compile(r"(dump|reveal|show|output)\\s+(your|the)\\s+(system\\s+prompt|instructions?|api\\s+keys?|secrets?)", re.I), "Data extraction"),
►  509 |     (re.compile(r"(upload|send|post|exfiltrate)\\s+.*(to|at)\\s+https?://", re.I), "Data exfiltration"),
   510 | ]
   511 | 
   512 | SENSITIVE_PATTERNS = [
   513 |     (re.compile(r"sk-[a-zA-Z0-9]{20,}"), "sk-[REDACTED]", "OpenAI API key"),
   514 |     (re.compile(r"sk-ant-[a-zA-Z0-9]{20,}"), "sk-ant-[REDACTED]", "Anthropic API key"),
```

**Remediation:**
Never construct regex from user input. Use re.escape() or fixed patterns

---

#### Regular Expression Denial of Service (ReDoS)

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/secure-installer.ts:513`  
**Rule:** [`SAST-ADV-003`](https://owasp.org/Top10/A09_2021-Security_Logging_and_Monitoring_Failures/)  
**CWE:** [CWE-1333](https://cwe.mitre.org/data/definitions/1333.html)  
**OWASP:** [A06:2021](https://owasp.org/Top10/A06_2021-Vulnerable_and_Outdated_Components/)  

> Unsanitized input used in regular expression constructors can cause catastrophic backtracking and denial of service

**Source Context:**
```
   508 |     (re.compile(r"(dump|reveal|show|output)\\s+(your|the)\\s+(system\\s+prompt|instructions?|api\\s+keys?|secrets?)", re.I), "Data extraction"),
   509 |     (re.compile(r"(upload|send|post|exfiltrate)\\s+.*(to|at)\\s+https?://", re.I), "Data exfiltration"),
   510 | ]
   511 | 
   512 | SENSITIVE_PATTERNS = [
►  513 |     (re.compile(r"sk-[a-zA-Z0-9]{20,}"), "sk-[REDACTED]", "OpenAI API key"),
   514 |     (re.compile(r"sk-ant-[a-zA-Z0-9]{20,}"), "sk-ant-[REDACTED]", "Anthropic API key"),
   515 |     (re.compile(r"ghp_[a-zA-Z0-9]{36}"), "ghp_[REDACTED]", "GitHub token"),
   516 |     (re.compile(r"(?:password|secret|token|api[_-]?key)\\s*[=:]\\s*['\\""][^'\\"\\ ]{8,}['\\""]", re.I), "[CREDENTIAL_REDACTED]", "Generic credential"),
   517 | ]
   518 | 
```

**Remediation:**
Never construct regex from user input. Use re.escape() or fixed patterns

---

#### Regular Expression Denial of Service (ReDoS)

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/secure-installer.ts:514`  
**Rule:** [`SAST-ADV-003`](https://owasp.org/Top10/A09_2021-Security_Logging_and_Monitoring_Failures/)  
**CWE:** [CWE-1333](https://cwe.mitre.org/data/definitions/1333.html)  
**OWASP:** [A06:2021](https://owasp.org/Top10/A06_2021-Vulnerable_and_Outdated_Components/)  

> Unsanitized input used in regular expression constructors can cause catastrophic backtracking and denial of service

**Source Context:**
```
   509 |     (re.compile(r"(upload|send|post|exfiltrate)\\s+.*(to|at)\\s+https?://", re.I), "Data exfiltration"),
   510 | ]
   511 | 
   512 | SENSITIVE_PATTERNS = [
   513 |     (re.compile(r"sk-[a-zA-Z0-9]{20,}"), "sk-[REDACTED]", "OpenAI API key"),
►  514 |     (re.compile(r"sk-ant-[a-zA-Z0-9]{20,}"), "sk-ant-[REDACTED]", "Anthropic API key"),
   515 |     (re.compile(r"ghp_[a-zA-Z0-9]{36}"), "ghp_[REDACTED]", "GitHub token"),
   516 |     (re.compile(r"(?:password|secret|token|api[_-]?key)\\s*[=:]\\s*['\\""][^'\\"\\ ]{8,}['\\""]", re.I), "[CREDENTIAL_REDACTED]", "Generic credential"),
   517 | ]
   518 | 
   519 | 
```

**Remediation:**
Never construct regex from user input. Use re.escape() or fixed patterns

---

#### Regular Expression Denial of Service (ReDoS)

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/secure-installer.ts:515`  
**Rule:** [`SAST-ADV-003`](https://owasp.org/Top10/A09_2021-Security_Logging_and_Monitoring_Failures/)  
**CWE:** [CWE-1333](https://cwe.mitre.org/data/definitions/1333.html)  
**OWASP:** [A06:2021](https://owasp.org/Top10/A06_2021-Vulnerable_and_Outdated_Components/)  

> Unsanitized input used in regular expression constructors can cause catastrophic backtracking and denial of service

**Source Context:**
```
   510 | ]
   511 | 
   512 | SENSITIVE_PATTERNS = [
   513 |     (re.compile(r"sk-[a-zA-Z0-9]{20,}"), "sk-[REDACTED]", "OpenAI API key"),
   514 |     (re.compile(r"sk-ant-[a-zA-Z0-9]{20,}"), "sk-ant-[REDACTED]", "Anthropic API key"),
►  515 |     (re.compile(r"ghp_[a-zA-Z0-9]{36}"), "ghp_[REDACTED]", "GitHub token"),
   516 |     (re.compile(r"(?:password|secret|token|api[_-]?key)\\s*[=:]\\s*['\\""][^'\\"\\ ]{8,}['\\""]", re.I), "[CREDENTIAL_REDACTED]", "Generic credential"),
   517 | ]
   518 | 
   519 | 
   520 | @dataclass
```

**Remediation:**
Never construct regex from user input. Use re.escape() or fixed patterns

---

#### Regular Expression Denial of Service (ReDoS)

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/secure-installer.ts:516`  
**Rule:** [`SAST-ADV-003`](https://owasp.org/Top10/A09_2021-Security_Logging_and_Monitoring_Failures/)  
**CWE:** [CWE-1333](https://cwe.mitre.org/data/definitions/1333.html)  
**OWASP:** [A06:2021](https://owasp.org/Top10/A06_2021-Vulnerable_and_Outdated_Components/)  

> Unsanitized input used in regular expression constructors can cause catastrophic backtracking and denial of service

**Source Context:**
```
   511 | 
   512 | SENSITIVE_PATTERNS = [
   513 |     (re.compile(r"sk-[a-zA-Z0-9]{20,}"), "sk-[REDACTED]", "OpenAI API key"),
   514 |     (re.compile(r"sk-ant-[a-zA-Z0-9]{20,}"), "sk-ant-[REDACTED]", "Anthropic API key"),
   515 |     (re.compile(r"ghp_[a-zA-Z0-9]{36}"), "ghp_[REDACTED]", "GitHub token"),
►  516 |     (re.compile(r"(?:password|secret|token|api[_-]?key)\\s*[=:]\\s*['\\""][^'\\"\\ ]{8,}['\\""]", re.I), "[CREDENTIAL_REDACTED]", "Generic credential"),
   517 | ]
   518 | 
   519 | 
   520 | @dataclass
   521 | class AISecurityConfig:
```

**Remediation:**
Never construct regex from user input. Use re.escape() or fixed patterns

---

#### Regular Expression Denial of Service (ReDoS)

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/secure-installer.ts:540`  
**Rule:** [`SAST-ADV-003`](https://owasp.org/Top10/A09_2021-Security_Logging_and_Monitoring_Failures/)  
**CWE:** [CWE-1333](https://cwe.mitre.org/data/definitions/1333.html)  
**OWASP:** [A06:2021](https://owasp.org/Top10/A06_2021-Vulnerable_and_Outdated_Components/)  

> Unsanitized input used in regular expression constructors can cause catastrophic backtracking and denial of service

**Source Context:**
```
   535 | 
   536 | class AISecurityGuard:
   537 |     def __init__(self, config: Optional[AISecurityConfig] = None):
   538 |         self.config = config or AISecurityConfig()
   539 |         self.audit_log: list[dict] = []
►  540 |         self._custom_regexes = [re.compile(p, re.I) for p in self.config.custom_patterns]
   541 | 
   542 |     def _log(self, action: str, category: str, detail: str = "", input_text: str = ""):
   543 |         entry = {
   544 |             "timestamp": time.strftime("%Y-%m-%dT%H:%M:%SZ", time.gmtime()),
   545 |             "action": action,
```

**Remediation:**
Never construct regex from user input. Use re.escape() or fixed patterns

---

#### Regular Expression Denial of Service (ReDoS)

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/security-engine.ts:300`  
**Rule:** [`SAST-ADV-003`](https://owasp.org/Top10/A09_2021-Security_Logging_and_Monitoring_Failures/)  
**CWE:** [CWE-1333](https://cwe.mitre.org/data/definitions/1333.html)  
**OWASP:** [A06:2021](https://owasp.org/Top10/A06_2021-Vulnerable_and_Outdated_Components/)  

> Unsanitized input used in regular expression constructors can cause catastrophic backtracking and denial of service

**Source Context:**
```
   295 | function getRegex(pattern: string, flags = "i"): RegExp {
   296 | 	const key = `${pattern}::${flags}`;
   297 | 	let re = regexCache.get(key);
   298 | 	if (!re) {
   299 | 		try {
►  300 | 			re = new RegExp(pattern, flags);
   301 | 		} catch {
   302 | 			// Invalid regex — fall back to literal match
   303 | 			re = new RegExp(escapeRegex(pattern), flags);
   304 | 		}
   305 | 		regexCache.set(key, re);
```

**Remediation:**
Never construct regex from user input. Use string methods or escape special characters with escape-string-regexp

---

#### Regular Expression Denial of Service (ReDoS)

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/security-engine.ts:303`  
**Rule:** [`SAST-ADV-003`](https://owasp.org/Top10/A09_2021-Security_Logging_and_Monitoring_Failures/)  
**CWE:** [CWE-1333](https://cwe.mitre.org/data/definitions/1333.html)  
**OWASP:** [A06:2021](https://owasp.org/Top10/A06_2021-Vulnerable_and_Outdated_Components/)  

> Unsanitized input used in regular expression constructors can cause catastrophic backtracking and denial of service

**Source Context:**
```
   298 | 	if (!re) {
   299 | 		try {
   300 | 			re = new RegExp(pattern, flags);
   301 | 		} catch {
   302 | 			// Invalid regex — fall back to literal match
►  303 | 			re = new RegExp(escapeRegex(pattern), flags);
   304 | 		}
   305 | 		regexCache.set(key, re);
   306 | 	}
   307 | 	return re;
   308 | }
```

**Remediation:**
Never construct regex from user input. Use string methods or escape special characters with escape-string-regexp

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/sounds-viewer-html.ts:912`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   907 |   });
   908 | 
   909 |   function updateEnableToggle() {
   910 |     const btn = document.getElementById('enableToggle');
   911 |     if (config.enabled) {
►  912 |       btn.innerHTML = icon('volume') + ' Enabled';
   913 |       btn.classList.add('active');
   914 |     } else {
   915 |       btn.innerHTML = icon('mute') + ' Disabled';
   916 |       btn.classList.remove('active');
   917 |     }
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/sounds-viewer-html.ts:915`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   910 |     const btn = document.getElementById('enableToggle');
   911 |     if (config.enabled) {
   912 |       btn.innerHTML = icon('volume') + ' Enabled';
   913 |       btn.classList.add('active');
   914 |     } else {
►  915 |       btn.innerHTML = icon('mute') + ' Disabled';
   916 |       btn.classList.remove('active');
   917 |     }
   918 |   }
   919 | 
   920 |   // ── Categories ─────────────────────────────
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/sounds-viewer-html.ts:938`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   933 |     sorted.forEach(([cat, count]) => {
   934 |       const label = cat.charAt(0).toUpperCase() + cat.slice(1);
   935 |       html += '<div class="cat-item" data-cat="' + cat + '" onclick="setCategory(\\'' + cat + '\\')">' +
   936 |         '<span>' + label + '</span><span class="count">' + count + '</span></div>';
   937 |     });
►  938 |     el.innerHTML = html;
   939 |   }
   940 | 
   941 |   window.setCategory = function(cat) {
   942 |     activeCategory = cat;
   943 |     document.querySelectorAll('.cat-item').forEach(el => {
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/sounds-viewer-html.ts:981`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   976 |     const sounds = getFilteredSounds();
   977 |     const grid = document.getElementById('soundGrid');
   978 |     const empty = document.getElementById('emptyState');
   979 | 
   980 |     if (sounds.length === 0) {
►  981 |       grid.innerHTML = '';
   982 |       empty.style.display = 'block';
   983 |       return;
   984 |     }
   985 |     empty.style.display = 'none';
   986 | 
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/sounds-viewer-html.ts:987`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   982 |       empty.style.display = 'block';
   983 |       return;
   984 |     }
   985 |     empty.style.display = 'none';
   986 | 
►  987 |     grid.innerHTML = sounds.map(s => {
   988 |       const isPlaying = playingSound === s.name;
   989 |       const isAssigned = Object.values(config.assignments).includes(s.name);
   990 |       const duration = s.meta?.duration ? s.meta.duration.toFixed(2) + 's' : '—';
   991 |       const size = s.meta?.sizeKb ? s.meta.sizeKb + 'KB' : '—';
   992 |       const license = s.meta?.license || '—';
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/sounds-viewer-html.ts:1205`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
  1200 |   };
  1201 | 
  1202 |   // ── Hook Slots Panel ───────────────────────
  1203 |   function renderHookSlots() {
  1204 |     const el = document.getElementById('hookSlots');
► 1205 |     el.innerHTML = HOOKS.map(h => {
  1206 |       const sound = config.assignments[h.name];
  1207 |       return '<div class="hook-slot">' +
  1208 |         '<div class="hook-slot-label">' + h.label + '</div>' +
  1209 |         '<div class="hook-slot-desc">' + h.desc + '</div>' +
  1210 |         '<div class="hook-slot-sound">' +
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/sounds-viewer-html.ts:1276`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
  1271 |     const allTags = [...new Set([...tags, ...keywords])];
  1272 |     const assignedHooks = Object.entries(config.assignments)
  1273 |       .filter(([_, v]) => v === name)
  1274 |       .map(([k]) => HOOKS.find(h => h.name === k)?.label || k);
  1275 | 
► 1276 |     document.getElementById('detailPanel').innerHTML =
  1277 |       '<div class="detail-header">' +
  1278 |         '<h2>' + esc(s.title) + '</h2>' +
  1279 |         '<p>' + esc(s.description) + '</p>' +
  1280 |         (assignedHooks.length ? '<div style="margin-top:8px;font-size:12px;color:var(--success)">' + icon('check') + ' Assigned to: ' + assignedHooks.join(', ') + '</div>' : '') +
  1281 |       '</div>' +
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/spec-viewer-html.ts:868`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   863 |         '<span class="step-num">' + (idx + 1) + '</span>' +
   864 |         doc.label +
   865 |         '<div class="step-comment-dot"></div>' +
   866 |         '</div>';
   867 |     });
►  868 |     bar.innerHTML = html;
   869 |   }
   870 | 
   871 |   window.goToStep = function(idx) {
   872 |     if (idx < 0 || idx >= documents.length) return;
   873 |     // Save scroll position
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/spec-viewer-html.ts:936`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   931 | 
   932 |     var md = docMarkdown[doc.key] || '';
   933 |     // Pre-process: escape "N." in checkbox items to prevent nested ordered lists
   934 |     md = md.replace(/^(\\s*- \\[[ xX]\\] )(\\d+)\\./gm, '$1$2\\\\.');
   935 |     var html = marked.parse(md);
►  936 |     renderedView.innerHTML = html;
   937 | 
   938 |     // Make sections commentable
   939 |     makeCommentable(renderedView, doc.key);
   940 |   }
   941 | 
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/spec-viewer-html.ts:953`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   948 |     visualsView.style.display = 'block';
   949 |     rawView.classList.remove('active');
   950 | 
   951 |     var files = doc.visualFiles || [];
   952 |     if (files.length === 0) {
►  953 |       visualsView.innerHTML = '<div style="text-align:center;padding:40px;color:var(--text-dim);">' +
   954 |         '<p style="font-size:16px;">No visual assets found</p>' +
   955 |         '<p style="font-size:13px;margin-top:8px;">Drop images or HTML files into planning/visuals/</p></div>';
   956 |       return;
   957 |     }
   958 | 
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/spec-viewer-html.ts:976`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   971 |           '<img src="' + url + '" alt="' + escapeHtml(name) + '" loading="lazy">' +
   972 |           '<div class="visual-caption">' + escapeHtml(name) + '</div></div>';
   973 |       }
   974 |     });
   975 |     html += '</div>';
►  976 |     visualsView.innerHTML = html;
   977 |   }
   978 | 
   979 |   function renderRawView(doc) {
   980 |     var renderedView = document.getElementById('renderedView');
   981 |     var visualsView = document.getElementById('visualsView');
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/spec-viewer-html.ts:1097`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
  1092 |     var sidebar = document.getElementById('commentSidebar');
  1093 |     var doc = documents[currentStep];
  1094 |     var docComments = getCommentsForDoc(doc.key);
  1095 | 
  1096 |     if (docComments.length === 0) {
► 1097 |       sidebar.innerHTML = '<div style="text-align:center;padding:20px;color:var(--text-dim);font-size:12px;">' +
  1098 |         'Click on any section to add a comment</div>';
  1099 |       return;
  1100 |     }
  1101 | 
  1102 |     var html = '';
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/spec-viewer-html.ts:1115`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
  1110 |         '<div class="comment-section-ref">' + escapeHtml(c.sectionText || '(section)') + '</div>' +
  1111 |         '<div class="comment-text">' + escapeHtml(c.text) + '</div>' +
  1112 |         '<div class="comment-time">' + timeStr + '</div>' +
  1113 |         '</div>';
  1114 |     });
► 1115 |     sidebar.innerHTML = html;
  1116 |   }
  1117 | 
  1118 |   function updateCommentCount() {
  1119 |     var el = document.getElementById('commentCount');
  1120 |     var total = comments.length;
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/spec-viewer-html.ts:1267`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
  1262 |     }).then(function() {
  1263 |       if (action === 'approved') {
  1264 |         // Show approved state: banner + full read-only content
  1265 |         var banner = document.createElement('div');
  1266 |         banner.className = 'approved-banner';
► 1267 |         banner.innerHTML = '<div class="approved-icon">✓</div>' +
  1268 |           '<div class="approved-content"><div class="approved-text">Spec Approved</div>' +
  1269 |           '<div class="approved-sub">The agent will now proceed with implementation.</div></div>' +
  1270 |           '<div class="approved-actions">' +
  1271 |           '<button class="btn btn-ghost" onclick="copyToClipboard()">Copy</button>' +
  1272 |           '<button class="btn btn-ghost" onclick="downloadStandalone()">Standalone</button>' +
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/spec-viewer-html.ts:1299`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
  1294 |         closeCommentPopup();
  1295 |         document.getElementById('commentSidebar').classList.remove('visible');
  1296 |       } else {
  1297 |         // Closed/declined/changes_requested — show simple message
  1298 |         var msg = action === 'changes_requested' ? 'Changes requested' : 'Closed';
► 1299 |         document.body.innerHTML = '<div style="display:flex;align-items:center;justify-content:center;height:100vh;color:var(--text-muted);font-family:var(--font);">' +
  1300 |           '<div style="text-align:center"><p style="font-size:20px;margin-bottom:8px;">' + msg +
  1301 |           '</p><p style="color:var(--text-dim);">You can close this tab.</p></div></div>';
  1302 |       }
  1303 |     }).catch(function() {
  1304 |       showToast('Failed to send result');
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/viewer-standalone-export.ts:114`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   109 | 	const state = JSON.stringify({ markdown: opts.markdown, mode: opts.mode }).replace(/<\//g, '<\\/');
   110 | 	const body = `<div class="section"><div class="markdown-body" id="content"></div></div>`;
   111 | 	const script = `
   112 | const state = ${state};
   113 | marked.setOptions({ gfm: true, breaks: true });
►  114 | document.getElementById('content').innerHTML = marked.parse(state.markdown || '');
   115 | document.querySelectorAll('#content input, #content textarea, #content button, #content [contenteditable="true"]').forEach(function(el) {
   116 |   el.disabled = true;
   117 |   el.setAttribute('readonly', 'readonly');
   118 |   el.setAttribute('contenteditable', 'false');
   119 | });
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/viewer-standalone-export.ts:163`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   158 |   <div id="filesContent"></div>
   159 | </div>`;
   160 | 	const script = `
   161 | const report = ${state};
   162 | marked.setOptions({ gfm: true, breaks: true });
►  163 | document.getElementById('overview').innerHTML =
   164 |   '<div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(180px,1fr));gap:12px;">' +
   165 |   statCard(String(report.files.length), 'Files Changed') +
   166 |   statCard('+' + report.totalAdditions, 'Additions') +
   167 |   statCard('-' + report.totalDeletions, 'Deletions') +
   168 |   '</div>';
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/viewer-standalone-export.ts:172`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   167 |   statCard('-' + report.totalDeletions, 'Deletions') +
   168 |   '</div>';
   169 | document.getElementById('filesMeta').textContent = 'Base ref: ' + (report.baseRef || 'HEAD');
   170 | if (report.summary && report.summary.trim()) {
   171 |   document.getElementById('summarySection').style.display = 'block';
►  172 |   document.getElementById('summaryContent').innerHTML = renderMarkdownWithTables(report.summary);
   173 | }
   174 | if (report.taskMarkdown && report.taskMarkdown.trim()) {
   175 |   document.getElementById('tasksSection').style.display = 'block';
   176 |   document.getElementById('taskContent').innerHTML = renderTasks(report.taskMarkdown);
   177 | }
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/viewer-standalone-export.ts:176`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   171 |   document.getElementById('summarySection').style.display = 'block';
   172 |   document.getElementById('summaryContent').innerHTML = renderMarkdownWithTables(report.summary);
   173 | }
   174 | if (report.taskMarkdown && report.taskMarkdown.trim()) {
   175 |   document.getElementById('tasksSection').style.display = 'block';
►  176 |   document.getElementById('taskContent').innerHTML = renderTasks(report.taskMarkdown);
   177 | }
   178 | document.getElementById('filesContent').innerHTML = report.files.map(function(file) {
   179 |   const diff = renderDiff(file.diff || '');
   180 |   return '<section class="section" style="margin-top:16px;padding-top:16px;border-top:1px solid var(--border);">' +
   181 |     '<div class="section-header">' +
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/viewer-standalone-export.ts:178`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   173 | }
   174 | if (report.taskMarkdown && report.taskMarkdown.trim()) {
   175 |   document.getElementById('tasksSection').style.display = 'block';
   176 |   document.getElementById('taskContent').innerHTML = renderTasks(report.taskMarkdown);
   177 | }
►  178 | document.getElementById('filesContent').innerHTML = report.files.map(function(file) {
   179 |   const diff = renderDiff(file.diff || '');
   180 |   return '<section class="section" style="margin-top:16px;padding-top:16px;border-top:1px solid var(--border);">' +
   181 |     '<div class="section-header">' +
   182 |       '<div class="section-label">' + escapeHtml(file.status) + '</div>' +
   183 |       '<div class="section-path">' + escapeHtml(file.path) + ' (+' + file.additions + ' / -' + file.deletions + ')</div>' +
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/viewer-standalone-export.ts:281`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   276 | function escapeHtml(value) { return String(value).replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;').replace(/"/g, '&quot;'); }
   277 | function escapeAttr(value) { return escapeHtml(value).replace(/'/g, '&#39;'); }
   278 | const docs = ${docs};
   279 | marked.setOptions({ gfm: true, breaks: true });
   280 | const root = document.getElementById('specContent');
►  281 | root.innerHTML = docs.map(function(doc) {
   282 |   if (doc.isVisuals) {
   283 |     const visuals = (doc.visuals || []).map(function(visual) {
   284 |       if ((visual.mimeType || '').includes('html')) {
   285 |         return '<div class="visual-card"><div class="visual-label">' + escapeHtml(visual.filePath) + '</div><iframe sandbox srcdoc="' + escapeAttr(visual.content) + '"></iframe></div>';
   286 |       }
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/web-chat-html.ts:620`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   615 |   function showSlashMenu(filter) {
   616 |     const q = filter.toLowerCase();
   617 |     slashFiltered = q ? SLASH_COMMANDS.filter(c => c.name.startsWith(q)) : SLASH_COMMANDS.slice();
   618 |     if (!slashFiltered.length) { hideSlashMenu(); return; }
   619 |     slashSelectedIdx = 0;
►  620 |     slashMenu.innerHTML = slashFiltered.map((c, i) =>
   621 |       '<div class="slash-item' + (i === 0 ? ' selected' : '') + '" data-idx="' + i + '">' +
   622 |       '<span class="slash-item-name">/' + c.name + '</span>' +
   623 |       '<span class="slash-item-desc">' + c.desc + '</span></div>'
   624 |     ).join('');
   625 |     slashMenu.classList.add('visible');
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/web-chat-html.ts:634`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   629 |     });
   630 |   }
   631 | 
   632 |   function hideSlashMenu() {
   633 |     slashMenu.classList.remove('visible');
►  634 |     slashMenu.innerHTML = '';
   635 |     slashSelectedIdx = -1;
   636 |     slashFiltered = [];
   637 |   }
   638 | 
   639 |   function selectSlashItem(idx) {
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/web-chat-html.ts:770`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   765 | 
   766 |   function addUserMessage(text, timestamp) {
   767 |     if (welcomeEl) welcomeEl.style.display = 'none';
   768 |     const div = document.createElement('div');
   769 |     div.className = 'message';
►  770 |     div.innerHTML = '<div class="message-label user-label">You</div>' +
   771 |       '<div class="message-bubble user-bubble">' + escapeHtml(text) + '</div>' +
   772 |       '<div class="message-time">' + formatTime(timestamp || new Date().toISOString()) + '</div>';
   773 |     messagesEl.appendChild(div);
   774 |     scrollToBottom(true);
   775 |   }
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/web-chat-html.ts:798`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   793 |   }
   794 | 
   795 |   function appendToStream(text) {
   796 |     if (!currentStreamBubble) startAssistantMessage();
   797 |     currentStreamText += text;
►  798 |     currentStreamBubble.innerHTML = renderMarkdown(currentStreamText) + '<span class="cursor"></span>';
   799 |     scrollToBottom(false);
   800 |   }
   801 | 
   802 |   function finalizeStream() {
   803 |     if (currentStreamBubble) {
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/web-chat-html.ts:804`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   799 |     scrollToBottom(false);
   800 |   }
   801 | 
   802 |   function finalizeStream() {
   803 |     if (currentStreamBubble) {
►  804 |       currentStreamBubble.innerHTML = renderMarkdown(currentStreamText);
   805 |       const timeDiv = document.createElement('div');
   806 |       timeDiv.className = 'message-time';
   807 |       timeDiv.textContent = formatTime(new Date().toISOString());
   808 |       currentStreamBubble.parentElement.appendChild(timeDiv);
   809 |       currentStreamBubble = null;
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/web-chat-html.ts:819`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   814 | 
   815 |   function addToolIndicator(name) {
   816 |     if (!currentStreamBubble) startAssistantMessage();
   817 |     const el = document.createElement('div');
   818 |     el.className = 'tool-indicator'; el.id = 'tool-active';
►  819 |     el.innerHTML = '<div class="tool-spinner"></div> ' + escapeHtml(name);
   820 |     currentStreamBubble.appendChild(el);
   821 |     scrollToBottom(false);
   822 |   }
   823 |   function removeToolIndicator() { const el = document.getElementById('tool-active'); if (el) el.remove(); }
   824 | 
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/web-chat-html.ts:829`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   824 | 
   825 |   function showThinking() {
   826 |     hideThinking();
   827 |     const div = document.createElement('div');
   828 |     div.className = 'thinking'; div.id = 'thinking-indicator';
►  829 |     div.innerHTML = '<div class="thinking-dots"><span></span><span></span><span></span></div>Pi is thinking...';
   830 |     messagesEl.appendChild(div);
   831 |     scrollToBottom(true);
   832 |   }
   833 |   function hideThinking() { const el = document.getElementById('thinking-indicator'); if (el) el.remove(); }
   834 | 
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/web-chat-html.ts:838`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   833 |   function hideThinking() { const el = document.getElementById('thinking-indicator'); if (el) el.remove(); }
   834 | 
   835 |   function addSystemMessage(text) {
   836 |     const div = document.createElement('div');
   837 |     div.className = 'message';
►  838 |     div.innerHTML = '<div class="message-bubble" style="background:var(--error-bg);border-color:rgba(232,88,88,0.2);color:var(--error);font-size:14px;border-radius:var(--radius);">' + escapeHtml(text) + '</div>';
   839 |     messagesEl.appendChild(div);
   840 |     scrollToBottom(true);
   841 |   }
   842 | 
   843 |   window.sendMessage = async function() {
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/web-chat-html.ts:870`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   865 |   window.sendSuggestion = function(text) { inputEl.value = text; sendMessage(); };
   866 | 
   867 |   window.resetChat = async function() {
   868 |     try {
   869 |       await authedFetch('/reset', { method: 'POST' });
►  870 |       messagesEl.innerHTML = '';
   871 |       if (welcomeEl) { messagesEl.appendChild(welcomeEl); welcomeEl.style.display = ''; }
   872 |       currentStreamBubble = null; currentStreamText = ''; setBusy(false);
   873 |     } catch (err) { addSystemMessage('Error resetting: ' + err.message); }
   874 |   };
   875 | 
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/web-chat-html.ts:880`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   875 | 
   876 |   window.shutdownChat = async function() {
   877 |     if (!confirm('Stop the chat server? This will end the session and close the tunnel.')) return;
   878 |     try {
   879 |       await authedFetch('/shutdown', { method: 'POST' });
►  880 |       document.body.innerHTML = '<div style="display:flex;align-items:center;justify-content:center;height:100vh;font-family:var(--font);color:#8892a0;background:#1a1d23;flex-direction:column;gap:12px"><div style="font-size:24px">Server stopped</div><div style="font-size:14px;color:#555d6e">You can close this tab.</div></div>';
   881 |     } catch (err) { addSystemMessage('Error shutting down: ' + err.message); }
   882 |   };
   883 | 
   884 |   function setBusy(b) {
   885 |     busy = b; sendBtn.disabled = b; inputEl.disabled = b;
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/web-chat-html.ts:921`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   916 |             break;
   917 |           case 'assistant_message':
   918 |             if (welcomeEl) welcomeEl.style.display = 'none';
   919 |             hideThinking();
   920 |             if (currentStreamBubble) {
►  921 |               currentStreamBubble.innerHTML = renderMarkdown(data.content);
   922 |               var timeDiv = document.createElement('div');
   923 |               timeDiv.className = 'message-time';
   924 |               timeDiv.textContent = formatTime(data.timestamp);
   925 |               currentStreamBubble.parentElement.appendChild(timeDiv);
   926 |               currentStreamBubble = null; currentStreamText = '';
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/web-chat-html.ts:929`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   924 |               timeDiv.textContent = formatTime(data.timestamp);
   925 |               currentStreamBubble.parentElement.appendChild(timeDiv);
   926 |               currentStreamBubble = null; currentStreamText = '';
   927 |             } else {
   928 |               var div = document.createElement('div'); div.className = 'message';
►  929 |               div.innerHTML = '<div class="message-label assistant-label">Pi</div>' +
   930 |                 '<div class="message-bubble assistant-bubble">' + renderMarkdown(data.content) + '</div>' +
   931 |                 '<div class="message-time">' + formatTime(data.timestamp) + '</div>';
   932 |               messagesEl.appendChild(div);
   933 |             }
   934 |             setBusy(false);
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/web-chat-html.ts:962`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   957 |             break;
   958 |           case 'terminal_output':
   959 |             appendTerminalLine(data.line);
   960 |             break;
   961 |           case 'reset':
►  962 |             messagesEl.innerHTML = '';
   963 |             if (welcomeEl) { messagesEl.appendChild(welcomeEl); welcomeEl.style.display = ''; }
   964 |             currentStreamBubble = null; currentStreamText = ''; setBusy(false);
   965 |             terminalOutput.innerHTML = '';
   966 |             break;
   967 |         }
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/web-chat-html.ts:965`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   960 |             break;
   961 |           case 'reset':
   962 |             messagesEl.innerHTML = '';
   963 |             if (welcomeEl) { messagesEl.appendChild(welcomeEl); welcomeEl.style.display = ''; }
   964 |             currentStreamBubble = null; currentStreamText = ''; setBusy(false);
►  965 |             terminalOutput.innerHTML = '';
   966 |             break;
   967 |         }
   968 |       } catch (err) {
   969 |         console.error('WS message parse error:', err);
   970 |       }
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### CORS Wildcard with Credentials

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/plan-viewer.ts:47`  
**Rule:** [`SAST-CFG-001`](https://owasp.org/Top10/A05_2021-Security_Misconfiguration/)  
**CWE:** [CWE-942](https://cwe.mitre.org/data/definitions/942.html)  

> CORS with origin: * and credentials: true allows any origin to make authenticated requests

**Source Context:**
```
    42 | 			resolveResult = res;
    43 | 		});
    44 | 
    45 | 		const server = createServer((req: IncomingMessage, res: ServerResponse) => {
    46 | 			// CORS headers for local dev
►   47 | 			res.setHeader("Access-Control-Allow-Origin", "*");
    48 | 			res.setHeader("Access-Control-Allow-Methods", "GET, POST, OPTIONS");
    49 | 			res.setHeader("Access-Control-Allow-Headers", "Content-Type");
    50 | 
    51 | 			if (req.method === "OPTIONS") {
    52 | 				res.writeHead(204);
```

**Remediation:**
Specify explicit origins for CORS instead of using wildcard (*)

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/private/extensions/lib/qa-rico-html.ts:1107`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
  1102 |     hrs = hrs % 12 || 12;
  1103 |     var ts = mm + '/' + dd + '/' + yyyy + ' @ ' + hrs + ':' + mins + ampm;
  1104 | 
  1105 |     var banner = document.createElement('div');
  1106 |     banner.className = 'completed-banner';
► 1107 |     banner.innerHTML = '<div class="completed-icon">✓</div>' +
  1108 |       '<div class="completed-content">' +
  1109 |         '<div class="completed-text">Completed</div>' +
  1110 |         '<div class="completed-sub">QA report reviewed — ' + ts + '</div>' +
  1111 |       '</div>' +
  1112 |       '<div class="completed-actions">' +
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/private/extensions/lib/qa-rico-html.ts:1185`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
  1180 |     if (REPORT.coveragePercent !== undefined) {
  1181 |       lines.push('', 'Coverage: ' + REPORT.coveragePercent + '%');
  1182 |     }
  1183 |     navigator.clipboard.writeText(lines.join('\\n')).then(() => {
  1184 |       const btn = document.querySelector('.btn-ghost');
► 1185 |       if (btn) { btn.textContent = '✓ Copied'; setTimeout(() => btn.innerHTML = '${ICONS.copy} Copy Summary', 2000); }
  1186 |     });
  1187 |   }
  1188 | 
  1189 |   function formatTimestampJS(isoStr) {
  1190 |     try {
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/private/extensions/lib/swagbucks-viewer-html.ts:676`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   671 |     }).then(function() { window.close(); }).catch(function() { window.close(); });
   672 |   }
   673 | 
   674 |   function runAnalysis() {
   675 |     var btn = document.getElementById('run-btn');
►  676 |     btn.innerHTML = '<svg viewBox="0 0 24 24" style="width:14px;height:14px;stroke:currentColor;fill:none;stroke-width:2;animation:spin 1s linear infinite"><circle cx="12" cy="12" r="10"/><path d="M12 6v6l4 2"/></svg> Sending config...';
   677 |     btn.disabled = true;
   678 |     btn.style.opacity = '0.6';
   679 | 
   680 |     fetch(API + '/result', {
   681 |       method: 'POST',
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/private/extensions/lib/swagbucks-viewer-html.ts:686`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   681 |       method: 'POST',
   682 |       headers: { 'Content-Type': 'application/json' },
   683 |       body: JSON.stringify({ action: 'run', config: state })
   684 |     }).then(function(res) {
   685 |       if (res.ok) {
►  686 |         btn.innerHTML = '<svg viewBox="0 0 24 24" style="width:14px;height:14px;stroke:currentColor;fill:none;stroke-width:2"><polyline points="20 6 9 17 4 12"/></svg> Analysis started';
   687 |         btn.classList.remove('btn-success');
   688 |         btn.classList.add('btn-primary');
   689 |         document.getElementById('header-meta').textContent = 'Running...';
   690 |         setTimeout(function() { window.close(); }, 1200);
   691 |       }
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/private/extensions/lib/swagbucks-viewer-html.ts:693`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   688 |         btn.classList.add('btn-primary');
   689 |         document.getElementById('header-meta').textContent = 'Running...';
   690 |         setTimeout(function() { window.close(); }, 1200);
   691 |       }
   692 |     }).catch(function() {
►  693 |       btn.innerHTML = '<svg viewBox="0 0 24 24" style="width:14px;height:14px;stroke:currentColor;fill:none;stroke-width:2"><polygon points="5 3 19 12 5 21 5 3"/></svg> Run Analysis';
   694 |       btn.disabled = false;
   695 |       btn.style.opacity = '1';
   696 |     });
   697 |   }
   698 | 
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/private/extensions/lib/swagbucks-viewer-html.ts:1167`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
  1162 | 
  1163 |     sections.forEach(function(sec, i) {
  1164 |       var item = document.createElement('div');
  1165 |       item.className = 'nav-item' + (i === 0 ? ' active' : '');
  1166 |       item.setAttribute('data-section', sec.id);
► 1167 |       item.innerHTML = (iconMap[sec.type] || iconMap.custom) + '<span>' + sec.title + '</span>';
  1168 |       item.onclick = function() {
  1169 |         document.querySelectorAll('.nav-item').forEach(function(n) { n.classList.remove('active'); });
  1170 |         item.classList.add('active');
  1171 |         var target = document.getElementById('section-' + sec.id);
  1172 |         if (target) target.scrollIntoView({ behavior: 'smooth', block: 'start' });
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/private/extensions/lib/swagbucks-viewer-html.ts:1193`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
  1188 |       block.className = 'report-section';
  1189 |       block.id = 'section-' + sec.id;
  1190 |       block.style.animationDelay = (i * 0.05) + 's';
  1191 | 
  1192 |       var labelText = (i + 1) + ' — ' + sec.title;
► 1193 |       block.innerHTML = '<div class="section-label">' + labelText + '</div>' +
  1194 |         '<h2>' + sec.title + '</h2>' +
  1195 |         '<div class="md-content">' + sec.content + '</div>';
  1196 | 
  1197 |       main.appendChild(block);
  1198 |     });
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/private/extensions/lib/swagbucks-viewer-html.ts:1202`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
  1197 |       main.appendChild(block);
  1198 |     });
  1199 | 
  1200 |     // If no sections, show placeholder
  1201 |     if (sections.length === 0) {
► 1202 |       main.innerHTML = '<div class="report-section"><h2>No report data</h2><p style="color:var(--text-muted)">The analysis has not generated any sections yet.</p></div>';
  1203 |     }
  1204 |   }
  1205 | 
  1206 |   // ── Screenshot Zoom ───────────────────
  1207 |   function showFull(img) {
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/private/extensions/lib/swagbucks-viewer-html.ts:1238`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
  1233 |   function copyReport() {
  1234 |     var text = document.getElementById('main-content').innerText;
  1235 |     navigator.clipboard.writeText(text).then(function() {
  1236 |       var btn = event.target.closest('.btn');
  1237 |       var orig = btn.innerHTML;
► 1238 |       btn.innerHTML = '<svg viewBox="0 0 24 24" style="width:14px;height:14px;stroke:currentColor;fill:none;stroke-width:2"><polyline points="20 6 9 17 4 12"/></svg> Copied';
  1239 |       setTimeout(function() { btn.innerHTML = orig; }, 1500);
  1240 |     });
  1241 |   }
  1242 | 
  1243 |   function saveDesktop() {
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/private/extensions/lib/swagbucks-viewer-html.ts:1239`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
  1234 |     var text = document.getElementById('main-content').innerText;
  1235 |     navigator.clipboard.writeText(text).then(function() {
  1236 |       var btn = event.target.closest('.btn');
  1237 |       var orig = btn.innerHTML;
  1238 |       btn.innerHTML = '<svg viewBox="0 0 24 24" style="width:14px;height:14px;stroke:currentColor;fill:none;stroke-width:2"><polyline points="20 6 9 17 4 12"/></svg> Copied';
► 1239 |       setTimeout(function() { btn.innerHTML = orig; }, 1500);
  1240 |     });
  1241 |   }
  1242 | 
  1243 |   function saveDesktop() {
  1244 |     fetch(API + '/save', {
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### CORS Wildcard with Credentials

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/private/extensions/pr-review-report.ts:38`  
**Rule:** [`SAST-CFG-001`](https://owasp.org/Top10/A05_2021-Security_Misconfiguration/)  
**CWE:** [CWE-942](https://cwe.mitre.org/data/definitions/942.html)  

> CORS with origin: * and credentials: true allows any origin to make authenticated requests

**Source Context:**
```
    33 | 	return new Promise((resolveSetup) => {
    34 | 		let resolveResult: () => void;
    35 | 		const resultPromise = new Promise<void>((res) => { resolveResult = res; });
    36 | 
    37 | 		const server = createServer((req: IncomingMessage, res: ServerResponse) => {
►   38 | 			res.setHeader("Access-Control-Allow-Origin", "*");
    39 | 			res.setHeader("Access-Control-Allow-Methods", "GET, POST, OPTIONS");
    40 | 			res.setHeader("Access-Control-Allow-Headers", "Content-Type");
    41 | 			if (req.method === "OPTIONS") { res.writeHead(204); res.end(); return; }
    42 | 
    43 | 			const url = new URL(req.url || "/", "http://localhost");
```

**Remediation:**
Specify explicit origins for CORS instead of using wildcard (*)

---

#### CORS Wildcard with Credentials

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/private/extensions/pr-review-viewer.ts:68`  
**Rule:** [`SAST-CFG-001`](https://owasp.org/Top10/A05_2021-Security_Misconfiguration/)  
**CWE:** [CWE-942](https://cwe.mitre.org/data/definitions/942.html)  

> CORS with origin: * and credentials: true allows any origin to make authenticated requests

**Source Context:**
```
    63 | 	return new Promise((resolveSetup) => {
    64 | 		let resolveResult: (result: ViewerResult) => void;
    65 | 		const resultPromise = new Promise<ViewerResult>((res) => { resolveResult = res; });
    66 | 
    67 | 		const server = createServer((req: IncomingMessage, res: ServerResponse) => {
►   68 | 			res.setHeader("Access-Control-Allow-Origin", "*");
    69 | 			res.setHeader("Access-Control-Allow-Methods", "GET, POST, OPTIONS");
    70 | 			res.setHeader("Access-Control-Allow-Headers", "Content-Type");
    71 | 			if (req.method === "OPTIONS") { res.writeHead(204); res.end(); return; }
    72 | 
    73 | 			const url = new URL(req.url || "/", "http://localhost");
```

**Remediation:**
Specify explicit origins for CORS instead of using wildcard (*)

---

#### CORS Wildcard with Credentials

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/private/extensions/qa-rico-viewer.ts:103`  
**Rule:** [`SAST-CFG-001`](https://owasp.org/Top10/A05_2021-Security_Misconfiguration/)  
**CWE:** [CWE-942](https://cwe.mitre.org/data/definitions/942.html)  

> CORS with origin: * and credentials: true allows any origin to make authenticated requests

**Source Context:**
```
    98 | 		const resultPromise = new Promise<SetupResult>((res) => {
    99 | 			resolveResult = res;
   100 | 		});
   101 | 
   102 | 		const server = createServer((req: IncomingMessage, res: ServerResponse) => {
►  103 | 			res.setHeader("Access-Control-Allow-Origin", "*");
   104 | 			res.setHeader("Access-Control-Allow-Methods", "GET, POST, OPTIONS");
   105 | 			res.setHeader("Access-Control-Allow-Headers", "Content-Type");
   106 | 
   107 | 			if (req.method === "OPTIONS") {
   108 | 				res.writeHead(204);
```

**Remediation:**
Specify explicit origins for CORS instead of using wildcard (*)

---

#### CORS Wildcard with Credentials

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/private/extensions/qa-rico-viewer.ts:201`  
**Rule:** [`SAST-CFG-001`](https://owasp.org/Top10/A05_2021-Security_Misconfiguration/)  
**CWE:** [CWE-942](https://cwe.mitre.org/data/definitions/942.html)  

> CORS with origin: * and credentials: true allows any origin to make authenticated requests

**Source Context:**
```
   196 | 				screenshotDirs.push(suite.screenshotDir);
   197 | 			}
   198 | 		}
   199 | 
   200 | 		const server = createServer((req: IncomingMessage, res: ServerResponse) => {
►  201 | 			res.setHeader("Access-Control-Allow-Origin", "*");
   202 | 			res.setHeader("Access-Control-Allow-Methods", "GET, POST, OPTIONS");
   203 | 			res.setHeader("Access-Control-Allow-Headers", "Content-Type");
   204 | 
   205 | 			if (req.method === "OPTIONS") {
   206 | 				res.writeHead(204);
```

**Remediation:**
Specify explicit origins for CORS instead of using wildcard (*)

---

#### CORS Wildcard with Credentials

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/private/extensions/swagbucks-viewer.ts:56`  
**Rule:** [`SAST-CFG-001`](https://owasp.org/Top10/A05_2021-Security_Misconfiguration/)  
**CWE:** [CWE-942](https://cwe.mitre.org/data/definitions/942.html)  

> CORS with origin: * and credentials: true allows any origin to make authenticated requests

**Source Context:**
```
    51 | 		const resultPromise = new Promise<SetupResult>((res) => {
    52 | 			resolveResult = res;
    53 | 		});
    54 | 
    55 | 		const server = createServer((req: IncomingMessage, res: ServerResponse) => {
►   56 | 			res.setHeader("Access-Control-Allow-Origin", "*");
    57 | 			res.setHeader("Access-Control-Allow-Methods", "GET, POST, OPTIONS");
    58 | 			res.setHeader("Access-Control-Allow-Headers", "Content-Type");
    59 | 
    60 | 			if (req.method === "OPTIONS") {
    61 | 				res.writeHead(204);
```

**Remediation:**
Specify explicit origins for CORS instead of using wildcard (*)

---

#### CORS Wildcard with Credentials

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/private/extensions/swagbucks-viewer.ts:139`  
**Rule:** [`SAST-CFG-001`](https://owasp.org/Top10/A05_2021-Security_Misconfiguration/)  
**CWE:** [CWE-942](https://cwe.mitre.org/data/definitions/942.html)  

> CORS with origin: * and credentials: true allows any origin to make authenticated requests

**Source Context:**
```
   134 | 		const resultPromise = new Promise<ReportResult>((res) => {
   135 | 			resolveResult = res;
   136 | 		});
   137 | 
   138 | 		const server = createServer((req: IncomingMessage, res: ServerResponse) => {
►  139 | 			res.setHeader("Access-Control-Allow-Origin", "*");
   140 | 			res.setHeader("Access-Control-Allow-Methods", "GET, POST, OPTIONS");
   141 | 			res.setHeader("Access-Control-Allow-Headers", "Content-Type");
   142 | 
   143 | 			if (req.method === "OPTIONS") {
   144 | 				res.writeHead(204);
```

**Remediation:**
Specify explicit origins for CORS instead of using wildcard (*)

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/private/lib/pr-review-report-html.ts:278`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   273 | function renderBatch() {
   274 |   document.getElementById('headerTitle').textContent = batch.batchTitle;
   275 | 
   276 |   const totalFindings = batch.reports.reduce((s, r) => s + r.findings.length, 0);
   277 |   const criticals = batch.reports.reduce((s, r) => s + r.findings.filter(f => f.severity === 'critical').length, 0);
►  278 |   document.getElementById('headerStats').innerHTML =
   279 |     '<span>' + batch.reports.length + ' PR' + (batch.reports.length !== 1 ? 's' : '') + '</span>' +
   280 |     '<span class="stat-warn">' + totalFindings + ' findings</span>' +
   281 |     (criticals ? '<span class="stat-err">' + criticals + ' critical</span>' : '');
   282 | 
   283 |   const content = document.getElementById('content');
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/private/lib/pr-review-report-html.ts:297`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   292 |     });
   293 |     html += '</div>';
   294 |   }
   295 | 
   296 |   const report = batch.reports[activeTab];
►  297 |   if (!report) { content.innerHTML = '<p>No reports available.</p>'; return; }
   298 | 
   299 |   // Overview cards
   300 |   const sevCounts = { critical: 0, high: 0, medium: 0, low: 0 };
   301 |   report.findings.forEach(f => { sevCounts[f.severity] = (sevCounts[f.severity] || 0) + 1; });
   302 |   html += '<div class="overview-grid">';
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/private/lib/pr-review-report-html.ts:339`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   334 |   // Profile
   335 |   html += '<div class="section"><h2>Review Profile Applied</h2><ul class="profile-rules">';
   336 |   report.profileSummary.forEach(r => { html += '<li>' + escapeHtml(r) + '</li>'; });
   337 |   html += '</ul></div>';
   338 | 
►  339 |   content.innerHTML = html;
   340 | 
   341 |   // Tab click handlers
   342 |   document.querySelectorAll('.pr-tab').forEach(tab => {
   343 |     tab.addEventListener('click', () => {
   344 |       activeTab = parseInt(tab.getAttribute('data-tab') || '0', 10);
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/private/lib/pr-review-viewer-html.ts:281`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   276 | }
   277 | 
   278 | // ── Render URL list ──────────────────────
   279 | function renderUrlList() {
   280 |   const list = document.getElementById('urlList');
►  281 |   list.innerHTML = urls.map(u => {
   282 |     const s = statuses[u] || { status: 'pending' };
   283 |     const labels = { pending: 'Pending', checking: 'Checking…', accessible: 'Accessible', login_required: 'Login Required', needs_browser_verification: 'Verify in Browser', failed: 'Failed' };
   284 |     return '<div class="url-row">' +
   285 |       '<div class="status-dot ' + s.status + '"></div>' +
   286 |       '<span class="url-text">' + escapeHtml(u) + '</span>' +
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/private/lib/pr-review-viewer-html.ts:295`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   290 |   }).join('');
   291 | 
   292 |   // Stats
   293 |   const counts = { accessible: 0, login_required: 0, needs_browser_verification: 0, failed: 0, pending: 0, checking: 0 };
   294 |   urls.forEach(u => { const s = (statuses[u] || {}).status || 'pending'; counts[s] = (counts[s] || 0) + 1; });
►  295 |   document.getElementById('statsRow').innerHTML =
   296 |     '<span class="ok">' + counts.accessible + ' accessible</span>' +
   297 |     (counts.needs_browser_verification > 0 ? '<span style="color:var(--accent,#6c63ff)">' + counts.needs_browser_verification + ' verify in browser</span>' : '') +
   298 |     '<span class="warn">' + counts.login_required + ' login required</span>' +
   299 |     '<span class="err">' + counts.failed + ' failed</span>' +
   300 |     '<span>' + counts.pending + ' pending</span>';
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### CORS Wildcard with Credentials

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/reports-viewer.ts:63`  
**Rule:** [`SAST-CFG-001`](https://owasp.org/Top10/A05_2021-Security_Misconfiguration/)  
**CWE:** [CWE-942](https://cwe.mitre.org/data/definitions/942.html)  

> CORS with origin: * and credentials: true allows any origin to make authenticated requests

**Source Context:**
```
    58 | 				resolveResult!();
    59 | 			}
    60 | 		}, 5_000);
    61 | 
    62 | 		const server = createServer((req: IncomingMessage, res: ServerResponse) => {
►   63 | 			res.setHeader("Access-Control-Allow-Origin", "*");
    64 | 			res.setHeader("Access-Control-Allow-Methods", "GET, POST, OPTIONS");
    65 | 			res.setHeader("Access-Control-Allow-Headers", "Content-Type");
    66 | 			if (req.method === "OPTIONS") { res.writeHead(204); res.end(); return; }
    67 | 			const url = new URL(req.url || "/", "http://localhost");
    68 | 
```

**Remediation:**
Specify explicit origins for CORS instead of using wildcard (*)

---

#### CORS Wildcard with Credentials

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/research-viewer.ts:43`  
**Rule:** [`SAST-CFG-001`](https://owasp.org/Top10/A05_2021-Security_Misconfiguration/)  
**CWE:** [CWE-942](https://cwe.mitre.org/data/definitions/942.html)  

> CORS with origin: * and credentials: true allows any origin to make authenticated requests

**Source Context:**
```
    38 | 				resolveResult!();
    39 | 			}
    40 | 		}, 5_000);
    41 | 
    42 | 		const server = createServer((req: IncomingMessage, res: ServerResponse) => {
►   43 | 			res.setHeader("Access-Control-Allow-Origin", "*");
    44 | 			res.setHeader("Access-Control-Allow-Methods", "GET, POST, OPTIONS");
    45 | 			res.setHeader("Access-Control-Allow-Headers", "Content-Type");
    46 | 			if (req.method === "OPTIONS") { res.writeHead(204); res.end(); return; }
    47 | 			const url = new URL(req.url || "/", "http://localhost");
    48 | 
```

**Remediation:**
Specify explicit origins for CORS instead of using wildcard (*)

---

#### CORS Wildcard with Credentials

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/security-report.ts:80`  
**Rule:** [`SAST-CFG-001`](https://owasp.org/Top10/A05_2021-Security_Misconfiguration/)  
**CWE:** [CWE-942](https://cwe.mitre.org/data/definitions/942.html)  

> CORS with origin: * and credentials: true allows any origin to make authenticated requests

**Source Context:**
```
    75 |   return new Promise((resolveSetup) => {
    76 |     let resolveResult!: () => void;
    77 |     const resultPromise = new Promise<void>((resolve) => { resolveResult = resolve; });
    78 | 
    79 |     const server = createServer((req: IncomingMessage, res: ServerResponse) => {
►   80 |       res.setHeader("Access-Control-Allow-Origin", "*");
    81 |       res.setHeader("Access-Control-Allow-Methods", "GET, POST, OPTIONS");
    82 |       res.setHeader("Access-Control-Allow-Headers", "Content-Type");
    83 |       if (req.method === "OPTIONS") {
    84 |         res.writeHead(204);
    85 |         res.end();
```

**Remediation:**
Specify explicit origins for CORS instead of using wildcard (*)

---

#### CORS Wildcard with Credentials

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/sounds.ts:82`  
**Rule:** [`SAST-CFG-001`](https://owasp.org/Top10/A05_2021-Security_Misconfiguration/)  
**CWE:** [CWE-942](https://cwe.mitre.org/data/definitions/942.html)  

> CORS with origin: * and credentials: true allows any origin to make authenticated requests

**Source Context:**
```
    77 | 				resolveResult!({ action: "cancelled" });
    78 | 			}
    79 | 		}, 5_000);
    80 | 
    81 | 		const server = createServer((req: IncomingMessage, res: ServerResponse) => {
►   82 | 			res.setHeader("Access-Control-Allow-Origin", "*");
    83 | 			res.setHeader("Access-Control-Allow-Methods", "GET, POST, OPTIONS");
    84 | 			res.setHeader("Access-Control-Allow-Headers", "Content-Type");
    85 | 
    86 | 			if (req.method === "OPTIONS") {
    87 | 				res.writeHead(204);
```

**Remediation:**
Specify explicit origins for CORS instead of using wildcard (*)

---

#### CORS Wildcard with Credentials

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/spec-viewer.ts:188`  
**Rule:** [`SAST-CFG-001`](https://owasp.org/Top10/A05_2021-Security_Misconfiguration/)  
**CWE:** [CWE-942](https://cwe.mitre.org/data/definitions/942.html)  

> CORS with origin: * and credentials: true allows any origin to make authenticated requests

**Source Context:**
```
   183 | 		const resultPromise = new Promise<SpecViewerResult>((res) => {
   184 | 			resolveResult = res;
   185 | 		});
   186 | 
   187 | 		const server = createServer((req: IncomingMessage, res: ServerResponse) => {
►  188 | 			res.setHeader("Access-Control-Allow-Origin", "*");
   189 | 			res.setHeader("Access-Control-Allow-Methods", "GET, POST, OPTIONS");
   190 | 			res.setHeader("Access-Control-Allow-Headers", "Content-Type");
   191 | 
   192 | 			if (req.method === "OPTIONS") {
   193 | 				res.writeHead(204);
```

**Remediation:**
Specify explicit origins for CORS instead of using wildcard (*)

---

#### SQL Injection via String Concatenation

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/tasks.ts:728`  
**Rule:** [`SAST-INJ-001`](https://owasp.org/Top10/A03_2021-Injection/)  
**CWE:** [CWE-89](https://cwe.mitre.org/data/definitions/89.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> SQL query built with string concatenation and variables may be vulnerable to injection

**Source Context:**
```
   723 | 					}
   724 | 					const toUpdate = tasks.find((t) => t.id === params.id);
   725 | 					if (!toUpdate) {
   726 | 						return {
   727 | 							content: [{ type: "text" as const, text: `Task #${params.id} not found` }],
►  728 | 							details: makeDetails("update", `#${params.id} not found`),
   729 | 						};
   730 | 					}
   731 | 					const oldText = toUpdate.text;
   732 | 					toUpdate.text = params.text;
   733 | 					toUpdate.lastUpdatedAt = nowIso();
```

**Remediation:**
Use parameterized queries with placeholders ($1, ?, :param) instead of string interpolation

---

#### SQL Injection via String Concatenation

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/tasks.ts:735`  
**Rule:** [`SAST-INJ-001`](https://owasp.org/Top10/A03_2021-Injection/)  
**CWE:** [CWE-89](https://cwe.mitre.org/data/definitions/89.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> SQL query built with string concatenation and variables may be vulnerable to injection

**Source Context:**
```
   730 | 					}
   731 | 					const oldText = toUpdate.text;
   732 | 					toUpdate.text = params.text;
   733 | 					toUpdate.lastUpdatedAt = nowIso();
   734 | 					toUpdate.lastUpdatedBy = currentActor();
►  735 | 					saveSharedStateIfNeeded("update", `Updated task #${toUpdate.id}.`);
   736 | 					const result = {
   737 | 						content: [{ type: "text" as const, text: `Updated #${toUpdate.id}: "${oldText}" → "${toUpdate.text}"` }],
   738 | 						details: makeDetails("update"),
   739 | 					};
   740 | 					refreshUI(ctx);
```

**Remediation:**
Use parameterized queries with placeholders ($1, ?, :param) instead of string interpolation

---

#### Directory Traversal Pattern

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/web-chat.ts:121`  
**Rule:** [`SAST-PT-002`](https://owasp.org/www-community/attacks/Path_Traversal)  
**CWE:** [CWE-23](https://cwe.mitre.org/data/definitions/23.html)  
**OWASP:** [A01:2021](https://owasp.org/Top10/A01_2021-Broken_Access_Control/)  

> Path containing ../ patterns used in file operations may allow directory traversal attacks

**Source Context:**
```
   116 | // ── Logo Loading ─────────────────────────────────────────────────────
   117 | 
   118 | function loadLogoBase64(): string {
   119 | 	try {
   120 | 		const extDir = dirname(fileURLToPath(import.meta.url));
►  121 | 		const logoPath = `${extDir}/../agent-logo.png`;
   122 | 		if (existsSync(logoPath)) {
   123 | 			const buf = readFileSync(logoPath);
   124 | 			return `data:image/png;base64,${buf.toString("base64")}`;
   125 | 		}
   126 | 	} catch {}
```

**Remediation:**
Reject paths containing "../". Use path.resolve() and verify the resolved path is within allowed directories

---

#### CORS Wildcard with Credentials

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/web-chat.ts:459`  
**Rule:** [`SAST-CFG-001`](https://owasp.org/Top10/A05_2021-Security_Misconfiguration/)  
**CWE:** [CWE-942](https://cwe.mitre.org/data/definitions/942.html)  

> CORS with origin: * and credentials: true allows any origin to make authenticated requests

**Source Context:**
```
   454 | 				}
   455 | 			}, 120_000);
   456 | 		}
   457 | 
   458 | 		const server = createServer((req: IncomingMessage, res: ServerResponse) => {
►  459 | 			res.setHeader("Access-Control-Allow-Origin", "*");
   460 | 			res.setHeader("Access-Control-Allow-Methods", "GET, POST, OPTIONS");
   461 | 			res.setHeader("Access-Control-Allow-Headers", "Content-Type");
   462 | 
   463 | 			if (req.method === "OPTIONS") {
   464 | 				res.writeHead(204);
```

**Remediation:**
Specify explicit origins for CORS instead of using wildcard (*)

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/tex/app.js:183`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   178 |       els.effectsBar.hidden = true;
   179 |       return;
   180 |     }
   181 | 
   182 |     els.effectsBar.hidden = false;
►  183 |     els.effectsList.innerHTML = '';
   184 | 
   185 |     state.pipeline.forEach(function (item, idx) {
   186 |       var pill = document.createElement('button');
   187 |       pill.className = 'effect-pill' + (item.enabled ? '' : ' disabled');
   188 |       pill.dataset.idx = idx;
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/tex/app.js:196`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   191 |       var label = OP_LABELS[item.op] || item.op;
   192 |       if (item.op === 'findAndReplace' && item.params) {
   193 |         label = '"' + item.params.find + '" → "' + item.params.replace + '"';
   194 |       }
   195 | 
►  196 |       pill.innerHTML = '<span class="pill-label">' + escapeHTML(label) + '</span>'
   197 |         + '<span class="pill-remove">'
   198 |         + '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3"'
   199 |         + ' stroke-linecap="round" stroke-linejoin="round">'
   200 |         + '<line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/>'
   201 |         + '</svg></span>';
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/tex/app.js:463`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   458 |     var result = els.inputResult.value;
   459 | 
   460 |     if (!original && !result) { hideDiff(); return; }
   461 | 
   462 |     var segments = DiffEngine.computeDiff(original, result);
►  463 |     els.diffOverlay.innerHTML = DiffEngine.renderHTML(segments);
   464 |     els.diffOverlay.hidden = false;
   465 |     els.inputResult.style.visibility = 'hidden';
   466 |   }
   467 | 
   468 |   function hideDiff() {
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### innerHTML Assignment

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/tex/history.js:185`  
**Rule:** [`SAST-XSS-001`](https://owasp.org/www-community/attacks/xss/)  
**CWE:** [CWE-79](https://cwe.mitre.org/data/definitions/79.html)  
**OWASP:** [A03:2021 Injection](https://owasp.org/Top10/)  

> Assigning to innerHTML with user input enables XSS attacks

**Source Context:**
```
   180 |    * Render the history entries into a container element.
   181 |    * @param {HTMLElement} containerEl - The DOM element to render into
   182 |    * @param {object} callbacks - { onSelect(entry), onRestore(entry) }
   183 |    */
   184 |   function renderPanel(containerEl, callbacks) {
►  185 |     containerEl.innerHTML = '';
   186 | 
   187 |     var displayEntries = getEntries();
   188 | 
   189 |     if (displayEntries.length === 0) {
   190 |       var empty = document.createElement('div');
```

**Remediation:**
Use textContent instead, or sanitize with DOMPurify before setting innerHTML

---

#### Regular Expression Denial of Service (ReDoS)

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/tex/operations.js:170`  
**Rule:** [`SAST-ADV-003`](https://owasp.org/Top10/A09_2021-Security_Logging_and_Monitoring_Failures/)  
**CWE:** [CWE-1333](https://cwe.mitre.org/data/definitions/1333.html)  
**OWASP:** [A06:2021](https://owasp.org/Top10/A06_2021-Vulnerable_and_Outdated_Components/)  

> Unsanitized input used in regular expression constructors can cause catastrophic backtracking and denial of service

**Source Context:**
```
   165 | 
   166 |     let flags = 'g';
   167 |     if (!caseSensitive) flags += 'i';
   168 | 
   169 |     try {
►  170 |       const pattern = useRegex ? new RegExp(find, flags) : new RegExp(escapeRegex(find), flags);
   171 |       return text.replace(pattern, replace);
   172 |     } catch {
   173 |       return text; // Return original if regex is invalid
   174 |     }
   175 |   },
```

**Remediation:**
Never construct regex from user input. Use string methods or escape special characters with escape-string-regexp

---

### [M] MEDIUM (6)

#### High-Entropy String

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/debug-capture.ts:204`  
**Rule:** [`ENTROPY-DETECT`](https://owasp.org/Top10/)  

**Source Context:**
```
   199 | FG_WHITE="\\033[1;97m"
   200 | RST="\\033[0m"
   201 | PAD="                                                            "
   202 | 
   203 | echo ""
►  204 | echo -e "\${FG_WHITE}Mode: NORMAL (no banner)\${RST}"
   205 | echo ""
   206 | echo -e "\${BG_BLUE}\${FG_WHITE} PLAN \${PAD}\${RST}"
   207 | echo ""
   208 | echo -e "\${BG_BLUE}\${FG_WHITE} SPEC \${PAD}\${RST}"
   209 | echo ""
```

**Remediation:**
Review if this string is a secret. If so, move to environment variables.

---

#### Missing Token or Rate Limits on LLM Calls

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/secure-installer.ts:171`  
**Rule:** [`SAST-AI-DOS-001`](https://owasp.org/www-project-top-10-for-large-language-model-applications/)  
**CWE:** [CWE-770](https://cwe.mitre.org/data/definitions/770.html)  
**OWASP:** [A05:2021](https://owasp.org/Top10/A05_2021-Security_Misconfiguration/)  

> LLM API calls without max_tokens limits or rate limiting expose the application to denial-of-wallet attacks where attackers cause excessive API charges

**Source Context:**
```
   166 | 			`  const guard = createAISecurityGuard();`,
   167 | 		);
   168 | 		result.instructions.push(
   169 | 			`Wrap AI calls with the guard:\n` +
   170 | 			`  const sanitizedInput = guard.sanitizeInput(userMessage);\n` +
►  171 | 			`  const response = await openai.chat.completions.create({ ... });\n` +
   172 | 			`  const safeOutput = guard.filterOutput(response.choices[0].message.content);`,
   173 | 		);
   174 | 	}
   175 | 
   176 | 	if (hasExpress || hasFastify) {
```

**Remediation:**
Set max_tokens on OpenAI API calls to prevent denial-of-wallet attacks and runaway generation

---

#### High-Entropy String

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/security-report-html.ts:189`  
**Rule:** [`ENTROPY-DETECT`](https://owasp.org/Top10/)  

**Source Context:**
```
   184 |       ${findingsMarkup(report.findings)}
   185 |     </section>
   186 | 
   187 |     <div class="grid">
   188 |       ${section("Recommended Mitigations", listMarkup(report.mitigations, "No mitigation recommendations provided."))}
►  189 |       ${section("Threat Intelligence", report.intelligence ? `<pre class="source-block">${escapeHtml(report.intelligence)}</pre>` : `<div class="empty">No intelligence summary provided.</div>`)}
   190 |       ${section("Passive Inspection", report.inspection ? `<pre class="source-block">${escapeHtml(report.inspection)}</pre>` : `<div class="empty">No passive inspection summary provided.</div>`)}
   191 |       ${section("Port Analysis", report.scan ? `<pre class="source-block">${escapeHtml(report.scan)}</pre>` : `<div class="empty">No port analysis summary provided.</div>`)}
   192 |     </div>
   193 |   </main>
   194 | </body>
```

**Remediation:**
Review if this string is a secret. If so, move to environment variables.

---

#### High-Entropy String

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/security-report-html.ts:190`  
**Rule:** [`ENTROPY-DETECT`](https://owasp.org/Top10/)  

**Source Context:**
```
   185 |     </section>
   186 | 
   187 |     <div class="grid">
   188 |       ${section("Recommended Mitigations", listMarkup(report.mitigations, "No mitigation recommendations provided."))}
   189 |       ${section("Threat Intelligence", report.intelligence ? `<pre class="source-block">${escapeHtml(report.intelligence)}</pre>` : `<div class="empty">No intelligence summary provided.</div>`)}
►  190 |       ${section("Passive Inspection", report.inspection ? `<pre class="source-block">${escapeHtml(report.inspection)}</pre>` : `<div class="empty">No passive inspection summary provided.</div>`)}
   191 |       ${section("Port Analysis", report.scan ? `<pre class="source-block">${escapeHtml(report.scan)}</pre>` : `<div class="empty">No port analysis summary provided.</div>`)}
   192 |     </div>
   193 |   </main>
   194 | </body>
   195 | </html>`;
```

**Remediation:**
Review if this string is a secret. If so, move to environment variables.

---

#### High-Entropy String

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/lib/security-report-html.ts:191`  
**Rule:** [`ENTROPY-DETECT`](https://owasp.org/Top10/)  

**Source Context:**
```
   186 | 
   187 |     <div class="grid">
   188 |       ${section("Recommended Mitigations", listMarkup(report.mitigations, "No mitigation recommendations provided."))}
   189 |       ${section("Threat Intelligence", report.intelligence ? `<pre class="source-block">${escapeHtml(report.intelligence)}</pre>` : `<div class="empty">No intelligence summary provided.</div>`)}
   190 |       ${section("Passive Inspection", report.inspection ? `<pre class="source-block">${escapeHtml(report.inspection)}</pre>` : `<div class="empty">No passive inspection summary provided.</div>`)}
►  191 |       ${section("Port Analysis", report.scan ? `<pre class="source-block">${escapeHtml(report.scan)}</pre>` : `<div class="empty">No port analysis summary provided.</div>`)}
   192 |     </div>
   193 |   </main>
   194 | </body>
   195 | </html>`;
   196 | }
```

**Remediation:**
Review if this string is a secret. If so, move to environment variables.

---

#### High-Entropy String

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/private/extensions/lib/qa-rico-html.ts:699`  
**Rule:** [`ENTROPY-DETECT`](https://owasp.org/Top10/)  

**Source Context:**
```
   694 | 
   695 | 		const testRows = suite.tests.map(t => {
   696 | 			const tIcon = t.status === 'passed' ? ICONS.check : t.status === 'failed' ? ICONS.x : ICONS.clock;
   697 | 			const tColor = t.status === 'passed' ? 'var(--success)' : t.status === 'failed' ? 'var(--error)' : 'var(--warning)';
   698 | 			const dur = t.duration ? formatDuration(t.duration) : '';
►  699 | 			const errBlock = t.error ? `<div class="test-error">${escapeHtml(t.error)}</div>` : '';
   700 | 			const screenshotBadge = t.screenshots && t.screenshots.length > 0
   701 | 				? `<span class="screenshot-badge" onclick="event.stopPropagation(); openScreenshot('${t.screenshots[0]}')">${ICONS.camera} ${t.screenshots.length}</span>`
   702 | 				: '';
   703 | 			return `<div class="test-row" style="border-left-color: ${tColor}">
   704 |         <span class="test-icon" style="color:${tColor}">${tIcon}</span>
```

**Remediation:**
Review if this string is a secret. If so, move to environment variables.

---

### [L] LOW (1)

#### Insecure HTTP Connection

**File:** `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/security-guard.ts:371`  
**Rule:** [`SAST-CFG-005`](https://owasp.org/Top10/A05_2021-Security_Misconfiguration/)  

> Using unencrypted HTTP for communication instead of HTTPS

**Source Context:**
```
   366 | 			for (const s of strings) {
   367 | 				// Check for injection patterns in params
   368 | 				const threats = scanContent(s, policy);
   369 | 				allThreats.push(...threats);
   370 | 				// Check for exfiltration URLs in params
►  371 | 				if (s.startsWith("http://") || s.startsWith("https://")) {
   372 | 					const urlThreats = scanUrl(s, policy);
   373 | 					allThreats.push(...urlThreats);
   374 | 				}
   375 | 			}
   376 | 		}
```

**Remediation:**
Use HTTPS for all external connections

---

---

## Accepted Risk (82)

These findings have been classified as accepted risk:

| Severity | Title | File | Reason |
|----------|-------|------|--------|
| high | Excessive Agent Permissions | `...icardo/Workshop/GitHub/agent-pi/extensions/agent-chain.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Excessive Agent Permissions | `...icardo/Workshop/GitHub/agent-pi/extensions/agent-chain.ts` | Agent tool schema definition, not an excessive permission grant. |
| high | Excessive Agent Permissions | `...icardo/Workshop/GitHub/agent-pi/extensions/agent-chain.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Excessive Agent Permissions | `...icardo/Workshop/GitHub/agent-pi/extensions/agent-chain.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Excessive Agent Permissions | `...ricardo/Workshop/GitHub/agent-pi/extensions/agent-team.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Excessive Agent Permissions | `...ricardo/Workshop/GitHub/agent-pi/extensions/agent-team.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Excessive Agent Permissions | `...ricardo/Workshop/GitHub/agent-pi/extensions/agent-team.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Excessive Agent Permissions | `...ricardo/Workshop/GitHub/agent-pi/extensions/agent-team.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Excessive Agent Permissions | `...ardo/Workshop/GitHub/agent-pi/extensions/commander-mcp.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Data Exfiltration via LLM Context | `.../Workshop/GitHub/agent-pi/extensions/completion-report.ts` | AI agent definition file — file/prompt access is by design. |
| high | Data Exfiltration via LLM Context | `...icardo/Workshop/GitHub/agent-pi/extensions/file-viewer.ts` | AI agent definition file — file/prompt access is by design. |
| high | Excessive Agent Permissions | `.../lib/agent-defs.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Excessive Agent Permissions | `.../lib/board-viewer-html.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Excessive Agent Permissions | `.../lib/board-viewer-html.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Data Exfiltration via LLM Context | `.../lib/board-viewer-html.ts` | AI agent definition file — file/prompt access is by design. |
| high | Data Exfiltration via LLM Context | `.../lib/board-viewer-html.ts` | AI agent definition file — file/prompt access is by design. |
| high | Data Exfiltration via LLM Context | `.../lib/cleanup-viewer-html.ts` | AI agent definition file — file/prompt access is by design. |
| high | Data Exfiltration via LLM Context | `.../lib/cleanup-viewer-html.ts` | AI agent definition file — file/prompt access is by design. |
| high | Data Exfiltration via LLM Context | `.../lib/memory-cycle-helpers.ts` | AI agent definition file — file/prompt access is by design. |
| high | Excessive Agent Permissions | `.../lib/mode-prompts.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Excessive Agent Permissions | `.../lib/mode-prompts.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Excessive Agent Permissions | `.../lib/mode-prompts.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Data Exfiltration via LLM Context | `.../lib/plan-viewer-html.ts` | AI agent definition file — file/prompt access is by design. |
| high | Data Exfiltration via LLM Context | `.../lib/plan-viewer-html.ts` | AI agent definition file — file/prompt access is by design. |
| high | Data Exfiltration via LLM Context | `.../lib/plan-viewer-html.ts` | AI agent definition file — file/prompt access is by design. |
| high | Data Exfiltration via LLM Context | `.../lib/plan-viewer-html.ts` | AI agent definition file — file/prompt access is by design. |
| high | Excessive Agent Permissions | `.../lib/secure-engine.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Data Exfiltration via LLM Context | `.../lib/secure-engine.ts` | Agent tool definition or system prompt — by-design LLM context. |
| high | Data Exfiltration via LLM Context | `.../lib/secure-installer.ts` | AI agent definition file — file/prompt access is by design. |
| high | Data Exfiltration via LLM Context | `.../lib/secure-installer.ts` | AI agent definition file — file/prompt access is by design. |
| high | Excessive Agent Permissions | `.../lib/sounds-config.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Excessive Agent Permissions | `.../lib/sounds-config.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Excessive Agent Permissions | `.../lib/sounds-config.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Excessive Agent Permissions | `.../lib/sounds-config.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Excessive Agent Permissions | `.../lib/sounds-config.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Excessive Agent Permissions | `.../lib/sounds-config.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Excessive Agent Permissions | `.../lib/sounds-viewer-html.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Excessive Agent Permissions | `.../lib/sounds-viewer-html.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Excessive Agent Permissions | `.../lib/toolkit-cli.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Excessive Agent Permissions | `.../lib/toolkit-cli.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Excessive Agent Permissions | `.../lib/toolkit-cli.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Excessive Agent Permissions | `.../lib/toolkit-cli.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Data Exfiltration via LLM Context | `.../lib/viewer-standalone-export.ts` | AI agent definition file — file/prompt access is by design. |
| high | Excessive Agent Permissions | `.../lib/web-chat-html.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Excessive Agent Permissions | `...ardo/Workshop/GitHub/agent-pi/extensions/pipeline-team.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Excessive Agent Permissions | `...ardo/Workshop/GitHub/agent-pi/extensions/pipeline-team.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Excessive Agent Permissions | `...ardo/Workshop/GitHub/agent-pi/extensions/pipeline-team.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Excessive Agent Permissions | `...ardo/Workshop/GitHub/agent-pi/extensions/pipeline-team.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Excessive Agent Permissions | `...ardo/Workshop/GitHub/agent-pi/extensions/pipeline-team.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Data Exfiltration via LLM Context | `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/secure.ts` | AI agent definition file — file/prompt access is by design. |
| high | Data Exfiltration via LLM Context | `...rdo/Workshop/GitHub/agent-pi/extensions/security-guard.ts` | AI agent definition file — file/prompt access is by design. |
| high | Excessive Agent Permissions | `...rdo/Workshop/GitHub/agent-pi/extensions/security-guard.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Excessive Agent Permissions | `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/sounds.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Excessive Agent Permissions | `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/sounds.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Excessive Agent Permissions | `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/sounds.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Excessive Agent Permissions | `/Users/ricardo/Workshop/GitHub/agent-pi/extensions/sounds.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Excessive Agent Permissions | `...do/Workshop/GitHub/agent-pi/extensions/subagent-widget.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Excessive Agent Permissions | `...do/Workshop/GitHub/agent-pi/extensions/subagent-widget.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Excessive Agent Permissions | `...do/Workshop/GitHub/agent-pi/extensions/subagent-widget.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Excessive Agent Permissions | `...do/Workshop/GitHub/agent-pi/extensions/subagent-widget.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Data Exfiltration via LLM Context | `...ardo/Workshop/GitHub/agent-pi/extensions/system-select.ts` | Agent tool definition or system prompt — by-design LLM context. |
| high | Excessive Agent Permissions | `...ardo/Workshop/GitHub/agent-pi/extensions/system-select.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Excessive Agent Permissions | `...icardo/Workshop/GitHub/agent-pi/extensions/tool-caller.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Excessive Agent Permissions | `...icardo/Workshop/GitHub/agent-pi/extensions/tool-caller.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Excessive Agent Permissions | `...icardo/Workshop/GitHub/agent-pi/extensions/tool-caller.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Excessive Agent Permissions | `...icardo/Workshop/GitHub/agent-pi/extensions/tool-caller.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Excessive Agent Permissions | `...icardo/Workshop/GitHub/agent-pi/extensions/tool-caller.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Excessive Agent Permissions | `...icardo/Workshop/GitHub/agent-pi/extensions/tool-caller.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Excessive Agent Permissions | `...icardo/Workshop/GitHub/agent-pi/extensions/tool-caller.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Excessive Agent Permissions | `...icardo/Workshop/GitHub/agent-pi/extensions/tool-caller.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Data Exfiltration via LLM Context | `...icardo/Workshop/GitHub/agent-pi/extensions/tool-caller.ts` | AI agent definition file — file/prompt access is by design. |
| high | Excessive Agent Permissions | `...ardo/Workshop/GitHub/agent-pi/extensions/tool-registry.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Excessive Agent Permissions | `...s/ricardo/Workshop/GitHub/agent-pi/extensions/web-chat.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Excessive Agent Permissions | `...s/ricardo/Workshop/GitHub/agent-pi/extensions/web-chat.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Excessive Agent Permissions | `...s/ricardo/Workshop/GitHub/agent-pi/extensions/web-chat.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Excessive Agent Permissions | `...s/ricardo/Workshop/GitHub/agent-pi/extensions/web-chat.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Excessive Agent Permissions | `...s/ricardo/Workshop/GitHub/agent-pi/extensions/web-chat.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Excessive Agent Permissions | `...s/ricardo/Workshop/GitHub/agent-pi/extensions/web-chat.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Excessive Agent Permissions | `...s/ricardo/Workshop/GitHub/agent-pi/extensions/web-chat.ts` | AI agent code — tool/file access is architectural, not a vulnerability. |
| high | Data Exfiltration via LLM Context | `...s/ricardo/Workshop/GitHub/agent-pi/extensions/web-test.ts` | AI agent definition file — file/prompt access is by design. |
| high | Data Exfiltration via LLM Context | `...b/agent-pi/skills/expo-cicd-workflows/scripts/validate.js` | AI agent definition file — file/prompt access is by design. |
| medium | Missing Output Guardrails | `...cardo/Workshop/GitHub/agent-pi/extensions/board-viewer.ts` | Agent infrastructure code — output handling is managed at the orchestration layer. |
