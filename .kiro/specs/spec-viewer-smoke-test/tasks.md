# Tasks

- [ ] 1. Open the smoke test spec in the spec viewer.
  - Verify `requirements.md` renders and includes the Context section.
- [ ] 2. Confirm the viewer does not throw `projectContext is not defined`.
  - Check that project metadata appears above Context when available.
