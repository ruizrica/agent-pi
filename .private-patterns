# Private content patterns — files that must NEVER appear in the public repo.
# Used by .githooks/pre-push and CI to block proprietary content from leaking.
# Format: one glob pattern per line (gitignore-style). Lines starting with # are comments.

# Session state (may contain proprietary context)
.context/session-state.json
